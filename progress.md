# 開発の記録（progress.md）

このPWAの開発経緯・現状・残課題のログです。新しい作業を行ったら追記してください。

## 2026-08-06 — 全国国語（第27回）に会場図を追加し、会場色を実地図に合わせた

開催初日。`events/2026-zenkokuken-27.json` に会場図（`assets/venue-map-2026-zenkokuken-27.svg`）を
足し、タイムテーブルの会場チップの色を**地図の色分けと一致**させた。

### 方角と色の対応

地図は方角ごとに塗り分けられている（テキストの方角ラベルも同色）。同じ会場を使う
全算研（`2026-zensanken-37.json`）が既に同じ規則で色を振っていたので、**そちらに合わせた**。

| 方角 | 地図の塗り | JSON の `color` |
| --- | --- | --- |
| 東 | `#D9F2D0`（緑） | `green` |
| 南・中央 | `#FBE3D6` / `#F6C6AD` / `#F2AA84`（赤〜橙） | `orange` / `orangeDeep` |
| 西校舎 | `#F2CFEE`（桃紫） | `purple` / `purpleDeep` |

`COLOR` 辞書（`index.html`）に**赤系は `venue-coral`（orange 系）しかない**ため、「南＝赤系」は
`orange`/`orangeDeep` で表現している。新パレットを足すより既存イベントとの一貫性を優先した。

### 直した不具合

- **6 部屋が `yellow` のままで、実際の所在と食い違っていた**ので上表に沿って割り当て直した。
  `yellow` は**校舎の「北」のテーマ色**として `tailwind.config` のパレット・`safelist`・`COLOR` 辞書・
  `validate_events.py` の `ROOM_COLORS` すべてに定義済みの**有効な色**で、壊れてはいない。
  ただしこの会場図に北の区画は無く、東・南・西の部屋に黄を当てていたので実態と合っていなかった。
  （作業中に一度「`yellow` は未定義でグレーになる」と誤って書いたが、`COLOR` 辞書を末尾まで
  読めば定義があり、事実ではない。）
- **`２部４年` を `orange` にしていたのが誤り**。地図の塗りは `#D9F2D0`＝東なので `green` が正しい。
  目視でなく**地図SVGの矩形の `fill` を正解データとして**全部屋を機械照合して見つけた
  （ラベル座標を含む最小の矩形を引く）。同じ手順で他 9 部屋は一致を確認済み。
- **`３部４年` も `green` に修正**。地図に同名のラベルが 2 つ（2階東＝緑／1階南＝赤系）あり
  機械照合では決められなかったが、**2階東が正**と確認が取れた。つまり地図側の 1階南のラベルが
  誤記で、そこが JSON の `４部３年` にあたる（`４部３年` は南なので `orange` のままで正しい）。
  これで JSON の部屋一覧と地図が 1 対 1 で対応する。
- **会場図SVGに `viewBox` が無かった**ので宣言サイズと同値の `viewBox` を足した。
  `<img class="w-full">` で幅可変に描くため、`viewBox` が無いと内容が拡縮されない解釈が起こりうる。
  宣言サイズと同値なので、既に正しく拡縮できている環境では表示が変わらない。

### 会場図の差し替えと、縦書きが崩れる問題

その後、事務局から**修正版の会場図**（`（２階東）`・`（１階東）` のラベル追加、階段の追記、
部屋名を 1 つの `<text>` にまとめる等）が届いたので差し替えた。あわせて、
**縦書きラベルが崩れる問題を全算研 SVG と同じ方法で直した**。

Office 系から書き出した SVG は縦書きを `writing-mode="tb-rl"` で表現するが、これは SVG 1.1 の
古い書き方で、ブラウザによって字送り・括弧の向きが崩れる。全算研の地図で採った対応に揃えて、
**`writing-mode` を使わず、1 文字＝1 `<tspan>` を縦に積む**形へ機械変換した。

- `x` は元の値のまま、`y` は `font-size` 刻み（37px なら `34.60 / 71.60 / 108.60 …`）
- `text-anchor="middle"` を付ける
- **括弧は縦書き用字形へ置換**（`（`→`︵`、`）`→`︶`）。横向きのまま残ると崩れて見える

変換後、5 つのラベル（`︵３階南︶` `︵１階南︶` `︵西校舎２階︶` `︵西校舎１階︶` `︵２階南︶`）が
全算研 SVG と同じ形式・同じ先頭 `y` になっていること、行送りが `font-size` と一致することを確認した。

差し替え後の地図でも会場色を再照合した。**塗り色と JSON の `color` は全部屋で一致**（新しく入った
`（２階東）`・`（１階東）` のラベルが、2階・1階の縦棒部分が東であることを裏づけていて、
`２部４年`・`３部４年` を緑にした判断とも整合する）。

### `CACHE_VERSION` を上げなかった理由

`sw.js` の `routeOf()` では会場図は `shell`（同一オリジンのその他）＝**Network First** なので、
版を上げなくても次回オンライン時に新しい SVG が入る。一方で版を上げると `SHELL_CACHE` を捨てて
**開催当日の会場回線でシェル一式を取り直させる**ことになるため、上げないほうが安全と判断した。
`index.html` は未変更。

### 地図の再修正と、A会場／B会場の解消

さらに**1階南のラベルを直した版**（`３部４年` の重複 → `４部３年`）が届いたので差し替えた。
これで地図の部屋と JSON の `rooms` が 1 対 1 で対応し、`３部４年`＝東・`４部３年`＝南
という色の割り当ても地図側から裏づけられた。

あわせて、宙に浮いていた **`A会場` / `B会場` を実際の部屋名へ置き換えた**。

| 旧表記 | 実際の会場 |
| --- | --- |
| `A会場` | `第１体育室` |
| `B会場` | `講堂` |

`rooms[]` の `A会場` / `B会場` は削除した（置き換え先の 2 部屋は既に定義済みで重複するため）。
`sessions` の `room` 参照に加えて、**協議会のタイトルの `公開授業Ⅱ A 協議会` /
`公開授業Ⅱ B 協議会` も部屋名へ差し替えた**（A/B という呼び方を残さない）。
置き換え後、`room` 参照がすべて `rooms[]` に存在し、未使用の定義も無いことを確認。
公開授業Ⅱ（13:00）とワークショップ（15:10 / 16:10）で同じ部屋を使うが、時間帯は重ならない。

表記は **`第１体育室` を正**とする。地図側のラベルが `第1体育館` だったので `第1体育室` に直した
（`体育館`→`体育室` は同じ 3 文字なので、後続の `)` の座標を動かさずに済む）。

### 会場図SVGの整形をスクリプト化

同じ変換を 3 回繰り返したので `scripts/fix_venue_map_svg.py` に切り出した。
`viewBox` の付与と縦書きの tspan 化をまとめて行う。次に地図が差し替わったら:

```bash
python3 scripts/fix_venue_map_svg.py <受領した.svg> assets/venue-map-<id>.svg
```

保存前に `xml.etree` でパースを通すので、壊れた SVG は書き出さない。

### 未解決（要確認）

- 全算研 JSON（`2026-zensanken-37.json`）は `第１体育館` 表記のまま。**別イベントなので触っていない**。
  会場は同じなので、次に触るときに `第１体育室` へ揃えるか確認したい。
- 全算研 JSON は部屋名を `３階東・１部６年` のように**方角つきで持っている**。全国国語も
  同じ形にすれば色に頼らず場所が伝わるが、開催中に表示名を変えるのは避けて見送った。

## 2026-08-04 — 一連の改修（#61/#62/#64）の見直しと不具合修正

LINE 導線（#61）・画像（#62）・待ち時間（#64）を一通り入れ終えたので、全体を読み直して
自分の実装の欠陥を洗った。**実バグ 3 件**と**不整合 3 件**が見つかったので直した。

### 1. `trimRuntime()` が「いちばん残したいもの」から消していた（実害あり）

設計時は「書影が青天井に増えるので FIFO で間引く」つもりだったが、**実装では書影を
`passthrough`（SW を通さない）にした**ので、書影はそもそもランタイムキャッシュに入らない。
実際に無制限に増えるのは**フォント実体**で、和文フォントは unicode-range で 100 以上の
サブセットに割れ、ウェイト 4 種だと簡単に上限へ達する。

一方 **Tailwind は install 時に最初に入り、イベント JSON もその直後に入る**。そこへ
挿入順の素朴な FIFO をかけると、**真っ先に消えるのがその 2 つ**になる。会場で効かせたい
本命を捨ててフォントのかけらを守る、という正反対の動作だった。

同じキー列（Tailwind＋イベント JSON＋フォント 200）で新旧を比較した実測:

| ポリシー | 削除数 | 巻き添えになった重要エントリ |
| --- | --- | --- |
| 旧（挿入順 FIFO・上限 120） | 82 件 | **Tailwind とイベント JSON の両方** |
| 新（`isTrimmable` で除外・上限 160） | 42 件 | **0 件** |

ブラウザ上でも、202 件まで埋めてから本物の `activate` を起こし、160 件へ削られたうえで
**Tailwind（挿入 1 番目）とイベント JSON（挿入 2 番目）が生存**することを確認した。

### 2. キャプティブポータルでキャッシュが汚染される（実害あり）

会場の Wi-Fi は **JSON の URL にも `200 text/html` でログイン画面を返す**。これは
オフラインバナーを `navigator.onLine` だけで判定しない理由として自分で挙げた状況そのものなのに、
取得側で無防備だった。素通しすると:

- そのログイン HTML が「イベント JSON」としてキャッシュへ保存される（次回の即時表示が壊れる）
- `markNetworkFresh()` まで走ってしまい、**取れていないのにオフライン表示が消える**

`index.html` の `fetchEventText()` で**キャッシュへ書く前に `JSON.parse` を通し**、失敗したら
throw して `onFail` へ回すようにした。`sw.js` 側も `looksJson()` で content-type を確かめてから保存する。
実測で、ポータル下でも保存済みの内容が出続け・キャッシュが汚れず・オフラインバナーが出ることを確認。

### 3. `再試行` で `init()` が多重実行される（実害あり）

`#errorRetry` の `location.reload()` をやめて `init()` の呼び直しにしたが、**呼び直せる＝多重実行できる**
という点を見落としていた。前回の取得が遅れて返ると両方の実行が描画へ進み、`showLinePromo()` が
二重に走ってスクロールリスナと 30 秒タイマーが 2 本ずつ張られる（ポップアップが 2 回開く）。
世代番号 `initRun` を持たせ、古い実行のコールバックは何もしないようにした。
再試行を素早く 3 回叩いても `showLinePromo` が 1 回以下であることを実測。

### 4〜6. 不整合の修正

- `coverUrl()` のホスト判定が `endsWith("toyokan.co.jp")` で、`eviltoyokan.co.jp` も拾っていた。
  完全一致＋`.toyokan.co.jp` に変更（`toyokan.co.jp.evil.com` も素通しになることを実測）。
- `sw.js` と `CLAUDE.md` のコメントが「書影がランタイムキャッシュに入る」と書いていて、
  実装（`passthrough`）と矛盾していた。将来の判断を誤らせるので実態に合わせた。
- `optimize_images.py` の `_save()` に使っていない引数 `dest` が残っていた。

既存の検証はすべて再実行して退行なし（キャッシュ即時表示 604ms・タブ/スクロール保持・
モーダル保留・503・遅延到着・壊れたキャッシュ・書影の 3 分岐）。
`validate_events.py` エラー0・警告0、テスト 12 件成功。シェル変更のため `CACHE_VERSION` を v107。

## 2026-08-04 — 弱電波の会場向け読み込み改善（2）待ち時間

（1）でバイト数を潰したので、残っていた**待ち時間**の側を直した。会場の回線は「切れている」より
**「つながっているが極端に遅い」**ことのほうが多く、素の Network First はこの状態がいちばん苦手だった。

**直した 4 つ**

1. **`sw.js` を 4 経路に分けた**（`routeOf()`）。版固定 URL（Tailwind / Google Fonts）は**キャッシュ優先で通信ゼロ**、
   書影は SW を通さず素通し、シェルは 2.5 秒でキャッシュに切替、`events/<id>.json` はネットワーク優先。
   **`eventJson` だけタイムアウトでキャッシュを返してはいけない**——ページ側が持つ差分検出が
   常に「変化なし」になって永久に更新されなくなる。鮮度の責任は 1 層だけが持つ。
   タイムアウトは `AbortSignal` ではなく `Promise.race`（abort すると裏のキャッシュ更新まで殺してしまう）。
2. **キャッシュを 2 本に分けた**。`SHELL_CACHE`（版ごと）と `RUNTIME_CACHE`（版をまたぐ）。
   従来は `CACHE_VERSION` を上げるたびに書影とイベント JSON まで捨てており、**シェル更新の直後だけ
   会場でまた遅くなっていた**。書影は青天井に増えるので `trimRuntime()` が FIFO で 120 件に間引く。
3. **`handleNavigate` のブロッキングを解消**。`getBrandColor` は未キャッシュ時に 34KB の JSON を
   **実際に fetch していた**ため、初回ナビゲーションがノッチの色のために 2 本目のリクエスト待ちで止まっていた。
   キャッシュ専用にし、色が無いときは 171KB の HTML のバッファと書き換えごと飛ばす（ストリーミングが生きる）。
4. **`init()` をキャッシュ即座表示に**。キャッシュから即描画 →裏で最新を取得 →生テキストの `===` で差分判定
   →変わっていれば静かに描き直し＋トースト。**「待ち切り」と「取得完了」を切り離した**のが要点——
   会場では「遅いが最終的には届く」が多く、締め切りで打ち切った分を捨てると更新が次回まで反映されないうえ、
   実際には通じているのにオフライン表示が残る。締め切りは待つのをやめるだけにした。

あわせて、**オフラインバナーが `navigator.onLine` だけを見ていた**問題も直した（会場の死んだ Wi-Fi では
`true` のままで、まさに想定した場面で出なかった）。`#errorRetry` の `location.reload()` もやめた
（シェルを取り直すと Tailwind CDN とフォントの代金を再度払うので、遅い回線ほど再試行が重くなる）。

**実測した結果**

検証用に**遅延・失敗を注入できる静的サーバ**（`/__ctl?delay=&fail=`）を書いた。**Service Worker 発の fetch は
Playwright の `page.route` を素通りする**ので、ブラウザ側からは「遅い回線」を作れなかったため。

| ケース | 結果 |
| --- | --- |
| 初回（キャッシュ無し） | 283 ms / キャッシュは `t-pod-runtime-1` と `t-pod-v106` の 2 本だけ |
| **2回目・JSON 5秒遅延** | **585 ms で表示**（改善前は 5 秒超、最悪 10 秒でエラー画面） |
| 内容が同じ | トーストを出さない |
| 内容が変わった | 即表示 →トースト →差し替え。**タブ books のまま／スクロール 800→800 で保持** |
| モーダルを開いている間 | 差し替えを保留し、閉じたら反映 |
| JSON が 503（キャッシュあり） | 本文表示・エラー画面なし・**オフラインバナーが出る**（`navigator.onLine` は true のまま） |
| **9秒遅延（締め切り6秒より遅い）** | 即表示 →6秒でオフライン表示 →**9秒で到着し表示が消え内容も更新**（取りこぼさない） |
| 壊れたキャッシュ | 真っ白にならず通常経路へ復帰 |
| 存在しない id | 従来どおりエラー画面。再試行は reload せず `init()` 再実行 |
| immutable 経路 | SW キャッシュの中身が使われ、**ネットワークへ 0 回** |
| `CACHE_VERSION` を v106→v107 | 新シェル生成・旧シェル削除・**ランタイムは中身ごと生存** |

再描画の安全性も実測: `setTab` は再描画で呼ばれない（手動の 1 回のみ）、`applyAppIdentity` は
識別値が変わった回だけ、`nowTimer` は同一 ID のまま（二重に走らない）。

**引っかかった点**: 更新トーストの中央寄せに `-translate-x-1/2` を使うと、登場演出の `fadeInUp` が
`transform` を `both` で握るため**演出後に左へ飛ぶ**。flex で中央寄せし、演出は中身のピルに当てる形にした。

`validate_events.py` エラー0・警告0、テスト12件成功。シェル変更のため `CACHE_VERSION` は v106。

## 2026-08-04 — 弱電波の会場向け読み込み改善（1）画像と外部リソース

「電波の弱い会場でも速く出したい」という要望を受けて計測したところ、**問題は2系統**に分かれた。

**バイト数**: 自前のコードは軽い（`index.html` gzip 53KB、`events/2026-zensanken-37.json` gzip 8.6KB）。
転送量は **画像と外部リソースにほぼ全部集中**していた。

| 資産 | 変更前 | 変更後 |
| --- | --- | --- |
| `assets/next-event-2026-zensanken-37.png/.webp` | 1,938,442 B | **124,542 B（-94%）** |
| `assets/next-event-2026-math-summer-fes.png` | 1,921,617 B | **333,992 B（-83%）** |
| 書影 22 枚（全算研） | 1200px 幅などのフルサイズ | 240px 幅の派生 |

**待ち時間**: `sw.js` の `networkFirst()` はキャッシュを `.catch()` でしか見ない＝ハード失敗時だけで、
「つながっているが遅い」ときは無限に待つ。`init()` も 10 秒 abort でキャッシュへ落ちないため、
同じ JSON が手元にあるのにエラー画面を出す。**こちらは大会終了後に別 PR で直す**（下記「残課題」）。

**今回入れた分（会期中に出せる安全な範囲）**——全算研2026 は 8/4-5 が本番で、
`nextEvent.showFrom` が **8/5 13:00**＝2日目の午後に 1.9MB のバナーが会場で一斉に落ちてくる。
そこで **Service Worker の挙動は変えず**（`CACHE_VERSION` を v105 に上げるのみ）、画像と `<head>` だけ直した。

- `scripts/optimize_images.py` を追加。幅 1400px へ縮小し、**現行イベントは WebP**、
  **終了済みイベントは同名 PNG のまま 256 色へ量子化**する（終了済みの JSON を編集しない規約を
  守るため拡張子を変えられない）。冪等で、既に小さければ書かない。生成画像は目視確認済みで、
  細い和文もグラデーションの帯も崩れていない。
- `coverUrl(url, px)` を追加し `renderBooks()` から使う。Shopify は `width=240`、Amazon は
  `_SL240_` / `MZZZZZZZ` へ。**JSON は無変更**なので既存・将来の全イベントに効く。
  **派生が 404 のときは元 URL で 1 回だけ retry** してからプレースホルダーに落ちる
  （外部 CDN のパラメータ仕様に賭けない）。書影に `width`/`height` 属性も付けた。
- バナーの箱を `aspect-[2/1]` で確保。**`width`/`height` 属性は使えない**——実寸がイベントごとに
  違い（1400×685 と 1400×788）、汎用シェルに固有の数値を持てないため。
- フォント CSS を `media="print"` → `onload` で `all` に戻す形にして**描画をブロックさせない**。
  ウェイト構成（400/500/700/900）は見た目を変えないため据え置き。`<noscript>` も置いた。
- `preconnect` に `cdn.tailwindcss.com`、`dns-prefetch` に画像ホスト3つを追加。
- 会場マップにも `loading="lazy"` / `decoding="async"`。

**実測した結果**（Playwright。外部ホストはプロキシで遮断されるので `page.route()` で代替。
**Service Worker が `page.route()` を迂回するので `serviceWorkers:'block'` が必要**だった）

| ケース | 結果 |
| --- | --- |
| `coverUrl()` 単体（3系統＋既に小さい＋未知ホスト＋想定外の形＋空） | すべて期待どおり。素通しも確認 |
| 書影・派生が正常 | 22枚とも派生URLで読み込み、retry 0 |
| 書影・派生が404 | 22枚とも元URLで retry し、**22枚とも表示された**（退行なし） |
| 書影・両方404 | 従来どおりプレースホルダー |
| バナー | WebP が 356×178 で表示、横スクロールなし |

Tailwind の実CSSをローカルビルド（実設定を抽出して 26KB）して差し込み、**見た目を実描画で確認**した。
ついでの発見として、**Play CDN の約120KB gzip ＋実行時 JIT に対しローカルビルドは 26KB**。
ビルド工程なしを優先して今回は CDN のままにしたが、将来の選択肢として CLAUDE.md に記録した。

`validate_events.py` エラー0・警告0、テスト12件成功。

**残課題（大会終了後・別PR）**: SW のキャッシュ分割（版付きシェル／長寿命ランタイム）と
4経路化（版固定URLはキャッシュ優先／`events/*.json` はネットワーク優先／シェルは2.5秒でキャッシュ）、
`handleNavigate` のブロッキング解消（`getBrandColor` をキャッシュ専用に）、
`init()` のキャッシュ即座表示＋差分検出で静かに再描画。設計は検討済み。

## 2026-08-04 — LINE内ブラウザ前提の「ホーム画面に追加」導線に作り直し

このサイトへの主要な流入は、**自社のLINE公式アカウントでキーワードを打つとURLが返る形式**
（例: `https://events.toyokan.co.jp/?id=2026-zensanken-37`）だと判明した。つまりほぼ全員が
**LINEのin-appブラウザで最初に開く**が、既存実装はSafari / Chromeで開かれている前提で、
この主要導線が3通りに破綻していた。

| 環境 | 修正前の挙動 |
| --- | --- |
| Android の LINE 内ブラウザ | `beforeinstallprompt` が来ず `isIos()` も false → `showInstallBanner()` が早期 return し**バナーごと出ない**。追加手段ゼロ |
| iOS の LINE 内ブラウザ | バナーは出るが、モーダルは「Safariの共有メニューから…」と案内する。**LINE内ブラウザの共有には「ホーム画面に追加」が無い**ので、その通りにしても追加できない |
| Android の Chrome | `deferredPrompt` が無いとき `installBtn` を押しても `isIos()` が false で**何も起きない**（無反応） |

**根本対策（LINE側のURL）**: LINE公式アカウントが送ったリンクは `openExternalBrowser=1` を付けると
LINE内ブラウザを経由せず端末のChrome / Safariで直接開く。`scripts/generate_event_url_index.py` に
「LINE配信用URL」列を足し、`docs/event-url-index.md` から貼り替え用URLをそのまま取れるようにした。
`getEventId()` は `URLSearchParams` で `id` だけを見るので余分なクエリは無害、生成マニフェストの
`start_url` も `./?id=<id>` 固定なのでパラメータは混入しない。LINE側の応答文の差し替えは運用作業。

**アプリ側（付け忘れ・古いリンク対策）**:

- `isAndroid()` / `isLineApp()`（UA の `Line/`。`Linux` は `/` が続かないので誤爆しない）/
  `isInAppBrowser()`（LINE＋Instagram・Facebook・X）を追加。
- `showInstallBanner()` の早期 return に in-app を含めず、Androidの LINE 内でもバナーが出るようにした。
  in-app のときは文言を「ブラウザで開くとアプリになります」／ボタン「開き方」に切り替える
  （`installTitle` / `installDesc` に id を付与）。`deferredPrompt` が来た場合は元の文言へ戻す。
- `iosInstallHelpHtml()` を `installHelpHtml()` に置き換え、**in-app×Android / in-app×iOS /
  iOS Safari / その他** の4分岐に。Androidは `chromeIntentUrl()`（`intent://…;package=com.android.chrome;`
  ＋ `S.browser_fallback_url` でChrome未導入時も落ちない）で「Chromeで開く」を主導線に、iOSは
  同等の手段が無いので「リンクをコピー」＋「⋯→Safariで開く」の手順を出す。
- `installBtn` のハンドラから `else if (isIos())` を外し、`deferredPrompt` が無ければ常に案内を開く
  （Android Chrome の無反応も同時に解消）。
- コピーは `navigator.clipboard.writeText()`、失敗時は readonly な `<input>` を差し込んで手動選択に落とす
  （WebViewでAPIが塞がれている場合の保険）。リスナは `openModal()` が innerHTML を入れた**後**に
  `openInstallHelp()` が張る（HTML文字列に `onclick` を書かない方針を維持）。
- モーダル内のボタンは淡地＋`color-mix(in srgb, var(--brand) 48%, black)` の文字色。
  **ブランド色ベタ塗り＋白文字は増やしていない**（明るい `brandColor` でAAを割るため）。
- LINE友だち追加ポップアップは `isLineApp()` のとき出さない（キーワード応答から来た人はすでに友だち）。
  localStorageは汚さないので、外部ブラウザで開き直したときは従来どおり出る。
- 案内ページ（`?id` なし）にも、LINEから開いた場合はブラウザで開き直す旨を1行足した。

シェル（HTML/JS）変更のため `CACHE_VERSION` を v103 → v104。`validate_events.py` エラー0・警告0、
`pytest` 12件成功。`events/*.json` は一切触っていない。

## 2026-07-31 — 全算研の次回イベント（冬季私学大会）バナー差し替え

`events/2026-zensanken-37.json` の `nextEvent.image` に、第27回冬季全国算数授業研究会 私学大会
（2027-01-16、暁星小学校）の告知バナーを設定した。画像はユーザーの Dropbox
（`04_全算研/270121-冬季全国算数授業研究大会/バナー/`）から提供を受け、既存の命名規則
（`assets/next-event-<id>.png`、`2026-math-summer-fes` の例に倣う）で `assets/next-event-2026-zensanken-37.png`
として取り込んだ。

- `nextEvent` の他フィールド（`title`/`catch`/`date`/`venue`/`description` 等）はバナーと内容が重複するが、
  既存の作法（画像＋テキストを両方出す）に合わせてそのまま残した。
- 画像は Service Worker のプリキャッシュ対象外（`sw.js` の `PRECACHE` リストに `assets/` 個別ファイルは
  無く、Network First でオンデマンド取得）のため `CACHE_VERSION` は据え置き。
- `validate_events.py` エラー0・警告0を確認。表示条件（`showFrom`: 2026-08-05T13:00+09:00 以降）は変更なし。

## 2026-07-31 — ふせんネコに「つまんで引っぱる」操作を追加

隠し要素として、指で掴んで動かせるドラッグと、勢いよく払ったときのフリック反応を足した。位置は
保存せず、離すと必ず定位置へバネで戻る「遊び」に限定したため、吹き出しの左右反転やレイアウト再設計、
localStorage の追加は不要だった。造形定義（`assets/fuseneko/fuseneko-grid.js`）は触らず、既存の
`surprise`／`lookR`／`lookL`／`blink` の組み合わせだけで「じたばた」「目を回す」を表現した。

- **DOM**: `#mascotBtn`（コマ送り・呼吸の transform を既に使用）とは別に、外側へ `#mascotHold` を
  ドラッグ専用の transform 層として追加（`--mascot-dx`/`--mascot-dy` を `translate3d` で反映）。
- **ジェスチャの一本化**: 「なでる（長押し 0.6 秒）」「タップ（開閉・名乗り）」に「ドラッグ」が加わり
  三つ巴になるため、`pointerdown/move/up/cancel` を 1 本の状態機械にまとめた。`petted`（次の click を
  1 回だけ捨てる目印）は意味が広がったので `swallowClick` に改名。
- **掴んでいる間の絵**: `CAT_HOLD`（しっぽを振るびっくり顔）を新設し `holdCatFrames()` が 16 コマ全部に
  敷く。`shuffleCatFrames()` は `catHeld` フラグを見て待機の差し替えを止め、`releaseCatFrames()` で
  戻す。フリック時は `dizzy`（`CAT_ACCENTS` に追加、待機プールには入れず反応専用）で目を回す。
- **タブスワイプとの分離**: `swipeEnabled()` に `#mascotBtn` の除外を追加（除外しないと同じ指を
  タブとネコが取り合う）。ただし `#mascotBtn { touch-action: pan-y }` にしたうえで、JS 側にも
  `DRAG_AXIS_RATIO`（1.4、タブスワイプと同値）による軸判定を入れた。**`touch-action` だけでは
  不十分**——ブラウザが「縦スクロールだ」と判断するのは数 px 動いた後で `DRAG_MIN_PX`（6px）と
  競合するため、軸判定なしだと縦に払っただけで一瞬ネコが持ち上がるちらつきが出る。
- **総合点検で見つけて直した不具合**: ①軸判定の実装漏れ（上記。プランには書いたが最初の実装では
  抜けていた）。②`#mascotHold` を素の `<span>` にした結果、中の `<button>`（inline-block）の行ボックスが
  ディセンダぶんの余白を持ち、`items-center` の中心が 2px（＝1ドット）ずれていた → `flex` を追加。
- **検証**: JS構文（`new Function()` によるパース）・`validate_events.py`（エラー0・警告0）・
  `tests/test_fuseneko.py`（7件成功、造形は無変更なので回帰なし）を確認。ブラウザでの実機確認は
  未実施（サンドボックス制約でローカルサーバを起動できなかったため、依頼者側での確認待ち）。
- シェル変更のため `CACHE_VERSION` を v102 → v103。

## 2026-07-27 — 「ふせんネコ図鑑」を削除

隠し要素の 1 つとして追加した図鑑（見出しを素早く 5 回タップ→全表情をモーダル表示）は、各セルが
`buildCatSvg()` の**静止 1 コマ**のみで、表情差分がほとんど 1〜2 ドットしかないため「見比べても違いが
分からない」との判断で削除した。他の隠し要素（3 回タップで自己紹介／長押しでなでる／深夜・早朝の一言）
は影響を受けず、そのまま残している。

- 削除: `CAT_GALLERY` 定数・`openCatGallery()`・`setupCatGallery()`／`catGalleryWired`・
  `TAPS_TO_GALLERY`・`.cat-gallery`/`.cat-cell`/`.cat-cell-art` の CSS・`#eventTitle` の
  `touch-manipulation`。`makeTapCounter()` は自己紹介でも使うため残した。
- **検証**: 見出しを素早く 5 回タップしても何も起きない／通常のタップ・スクロールに変化なし／
  残した 3 つの隠し要素に回帰なし／コンソールエラーなし（`CAT_GALLERY` 等の未定義参照が残っていないこと
  を確認）。`validate_events.py` エラー 0・警告 0、テスト 12 件成功。
- `CACHE_VERSION` を v99 → v100。

## 2026-07-27 — 隠し要素の見直し（自己レビューでの修正）

前節（PR #59）で入れた隠し要素を通しで見直し、見つかった 4 点を直した。挙動は変えていない。

- **例外で開閉ごと死ぬ穴**: `reactCatFrames()` は `CAT_ACCENTS[name] || CAT_RARES[name]` を引くようにしたので、**知らない名前で呼ぶと `frames` が undefined のまま `frames.length` で例外**になり、呼び出し元（吹き出しの開閉）まで巻き添えになる。`if (!frames) return;` を足した。
- **「素早く N 回」の数えが 2 箇所に重複**していたので `makeTapCounter(times)` に集約（間隔と「達したら 0 に戻す」を 1 箇所に）。
- **コントラスト**: 図鑑の説明文が `slate-400`＝白地に **2.56:1** で AA 未達だった（canvas で実測）。`slate-500`＝**4.76:1** に変更。セルのラベルも同じ 4.76:1。
- **ダブルタップ拡大**: 見出しは 5 回タップの入口なので `touch-manipulation` を当て、拡大が挟まらないようにした（ピンチ拡大は従来どおり）。
- ついでに、**復元時に localStorage へ書き戻さない**ようにした（`setFolded(folded, false)`）。触っていない人のストレージにキーを作らない。
- **検証**: 新規プロファイルで読み込んでも `t-pod:mascot-folded` が作られない／`reactCatFrames("nope")` が例外を出さない／3 回タップの自己紹介・4 回目で再発火しない・長押しのなで・短い押下の開閉・図鑑（15 セル、ゆっくり 5 回では開かない、1 回タップでも開かない）・深夜/早朝の一言（02:00・06:00 のみ、07:00 と 12:00 と `live` では出ない）に回帰なし。`prefers-reduced-motion` でも同じ。エラー 0・テスト 12 件成功。`CACHE_VERSION` を v98 → v99。

## 2026-07-27 — マスコットの名前「ふせんネコ」を画面に出す

- 名前がコメントとドキュメント、配布用 SVG の `<title>` にしかなく、**アプリの画面には一度も出ていなかった**ため、隠し要素と読み上げの 2 経路で入れた。
- **隠し要素**: `TAP_GAP_MS`（1.2 秒）以内に `TAPS_TO_INTRODUCE`（3 回）つつくと「ぼく、ふせんネコ！」と名乗る（わくわく表情つき）。ふだんの 1 タップは従来どおり吹き出しの開閉のまま。
  - 名乗るときは吹き出しを強制的に開くので、折りたたみの 3 点セット（class・`aria-expanded`・localStorage）を `setFolded()` に集約して初期化・タップ・自己紹介の 3 経路から通した（以前は 2 経路に散っていた）。
  - 文言は `mascotPinUntil`（8 秒）の間だけ 7 秒タイマーを休ませて残す。`mascotMsgs`/`mascotMsgIdx` は触らないので、解除後は次の一言へ自然に送られて消える（状態を汚さない）。
  - 4 回目以降の連打で名乗り続けないよう、発火時にカウンタを 0 へ戻す。
- **常設**: `#mascotBtn` の `aria-label`／`title` を「ふせんネコ（タップで吹き出しを開閉）」に。読み上げとPCのホバーで名前が分かる（以前は「マスコット」）。
- 造形は変えていないので `assets/fuseneko/*` と生成 SVG は無変更（`build_fuseneko.py` の再実行も不要）。
- **検証（Playwright / `?id=2026-zensanken-37`）**: ゆっくり 1 タップ＝開閉のみで文言は不変／素早く 3 タップ＝強制的に開いて自己紹介・コマが反応の絵に差し替わる／4 回目＝再発火せず通常の開閉／自己紹介は 8〜14 秒で通常の文言へ復帰／`prefers-reduced-motion` では絵は不変のまま自己紹介だけ出る／折りたたみ状態の保存に回帰なし。`validate_events.py` エラー 0・警告 0、テスト 12 件成功。
- シェル変更のため `CACHE_VERSION` を v96 → v97。

**続けて隠し要素を 3 つ追加**（同じ PR）。

- **なでる（長押し 0.6 秒）**: 「ごろごろ…」＋ハートの来客を確定で流す。`reactCatFrames()` の参照先を `CAT_ACCENTS[name] || CAT_RARES[name]` に広げただけで来客も同じ枠に乗った。**離すと `click` が続けて飛んでくるので 1 回だけ捨てる**（捨てないと吹き出しが畳まれる）。click が来ない離し方に備えて、次の `pointerdown` でも目印を下ろす。
- **ふせんネコ図鑑**: `#eventTitle` を素早く 5 回つつくと `openModal()` で全 15 種（表情 11＋来客 4）を並べる。`buildCatSvg()` をそのまま使うので追加の通信ゼロ・ブランド色に自動追従。アプリ内で唯一使われていなかった「まじめ」もここで見られる。`fuseneko.js`／`variants.js` は dev ページ専用で読み込まないため、ラベルは `CAT_GALLERY` 側に持つ。ドット絵は 24 の整数倍（48px 固定）。
- **深夜・早朝の一言**: `mascotState()` の全分岐が通る `state()` を `withNightLine()` で包んだので、追加は 1 箇所だけ。0〜5 時「まだ起きてるの？」／5〜7 時「はやおきだね！」。進行中（`live`）と文言が空のときは足さない。
- **検証（Playwright）**: 長押し＝「ごろごろ…」・吹き出しが畳まれない・コマがハートに差し替わる／短い押下は従来どおり開閉／`pointercancel` では発火しない／click の来ない離し方の直後でも次のタップが効く／見出し 5 回タップで図鑑（15 セル・48×48・全セルに `<rect>` あり）、ゆっくり 5 回では開かない／Escape で閉じる／ルート（`?id` なし）では見出しを 5 回つついても何も起きない／`prefers-reduced-motion` では絵は静止のまま文言だけ出る／`mascotLines()` は JST 02:00・06:00 でのみ一言が増え、12:00 と `live` では増えない。実行時エラーなし。
- **測定時の注意**: この検証環境では Tailwind Play CDN が遮断されユーティリティクラスが当たらないため、**座標指定のクリック（`mouse.down/up`）が要素に当たらない**。`dispatchEvent` でポインタ列を送るか、`page.click(selector)` を使う。図鑑のレイアウトは `<style>` に書いた自前 CSS なので影響を受けない。
- `CACHE_VERSION` を v97 → v98。

## 2026-07-17 — 参加者向け一覧の廃止と開発者用URL台帳

- ルートURLのイベント一覧を廃止し、LINEまたはQRコードから個別イベントへアクセスする案内に変更。URLの転載・共有を控える文言は通常閲覧を妨げないよう概要・お知らせモーダル最下部に置き、全ページへ `noindex, nofollow, noarchive` を設定した。
- `events.json` は開発・検証用の索引として維持し、公開ルートからの取得とService Workerのプリキャッシュを停止した。
- `docs/event-url-index.md` を `events.json` から決定的に生成するスクリプトを追加。Excel取込時の自動更新と、検証時の差分検出に対応した。
- ルートの参加者向け案内に、東洋館出版社公式LINEの友だち追加ボタンを追加した。
- 第37回全国算数授業研究大会の「事後アンケート（参加者全員）」と「当日資料一式（PDF）」は、確定前の仮URLが設定されていたためイベントJSONから一時削除して非表示にした。各公開URLの確定後に同じ項目名・説明で再追加する。

## 現状サマリ
- **マルチイベント対応のタイムテーブル PWA が完成**し、`main` にマージ済み。
- 公開URL:
  - 参加者向け案内: `https://events.toyokan.co.jp/`
  - 個別イベント: `https://events.toyokan.co.jp/?id=2026-zensanken-37`
  - 作成済みURL一覧は公開画面に出さず、`docs/event-url-index.md` で開発者向けに管理する。
- 現在登録されているイベント: **第37回 全国算数授業研究大会**（`events/2026-zensanken-37.json`、amber `#f59e0b`）／**第27回 全国国語授業研究大会**（`events/2026-zenkokuken-27.json`、明るいシアン `#00b8c8`）／**オール筑波 算数サマーフェスティバル2026**（`events/2026-math-summer-fes.json`、スカイブルー `#0ea5e9`）の3件。
- 現在のデザイン: ロゴ調のコンパクトなヘッダー（**スクロールで縮小し、縮小時は半透明＋ぼかし**）、フッターは **TIMETABLE / FILES / BOOKS** の3タブ。左右スワイプ中は現在ビューと隣接ビューが指に追従し、距離または速度で切替を確定する。ボトムナビ・日付タブは**すりガラス調**（半透明＋blur、非対応環境は不透明にフォールバック）。タブ切替・タップ・現在セッションの強調は短時間の軽量演出で、`prefers-reduced-motion` では停止する。テーマ色は `eventInfo.brandColor`、任意の補助色は `eventInfo.brandColor2` でイベント毎に指定し、自動補正せず表示に使用する。BOOKS は `eventInfo.bookSale` があれば特設販売ページへの導線を表示する。ヘッダー2行目は `title`（正式名称）優先、テーマは `――` 等の区切りでサブテーマを自動改行。クレジット表記を全タブと参加者向け案内の本文最下部に表示し、概要カードには指定された主催／共催／協力／後援を表示する。
- **右下のマスコット（ふせんネコ）**: 24×24 のドット絵と吹き出しの枠をコードのドットデータから実行時生成し、色はブランド色に追従する（バイナリ資産なし）。文言は `mascotState()` が開催日・セッション・現在時刻から算出する純関数で、場面（開催前／当日／進行中／終了後／終了済み）に応じて内容とアニメの速さが変わる。待機アニメは 16 コマを 1 周ごとにランダムで組み直し、ふだんの動き 8 種とまれな来客 4 種（ちょう・とり・ハート・おんぷ）が出る。タップで吹き出しを畳め（状態は `localStorage`）、`eventInfo.mascot: false` でイベント単位に無効化できる。`prefers-reduced-motion` では 1 コマ目で静止する。
- LINE導線は、ルートの参加者向け案内では友だち追加ボタンを常設し、イベントページでは `eventInfo.linePromo: true` の場合だけポップアップを表示する（現行3イベントは `false`）。
- 全ページを `noindex, nofollow, noarchive` とし、URLの転載・共有を控える文言は概要・お知らせモーダル最下部だけに表示する。
- 現行の Service Worker `CACHE_VERSION` は `sw.js` を参照（この節に固定値を書くと更新の都度陳腐化するため記載しない）。過去の節にあるバージョン番号は、その変更時点の履歴として残している。

## マイルストーン（PR単位）

### PR #1 — 初期PWA構築（merged）
- ディレクトリ構成・レスポンシブUI（Tailwind CDN）・JSONパース・Service Worker を実装。
- ボトムナビ3タブ（プログラム / 資料 / 書籍）、Sticky ヘッダー、FAB、各種モーダル。
- Service Worker による Network First キャッシュ（オフライン対応）。
- サンプル `data.json`（架空イベント）を同梱。

### PR #2 — 実データ反映 + UI 汎用化（merged）
- 第37回 全国算数授業研究大会の実データ（テーマ・会期・会場・1〜2日目の全プログラム・登壇者・2日目ワークショップ案内）を反映。
- タイムテーブルを **room×time グリッド → 縦型タイムライン**（時間 → 内容 → 詳細）に変更。全体会中心＋一部 A〜D 会場の分科会に対応。
- `index.html` から固有文言を排除し、タイトル等を JSON から動的反映する**汎用シェル化**。`manifest.json` も汎用名に。

### PR #3 — マルチイベント対応（merged）
- `?id=<id>` クエリパラメータ方式を導入。`events/<id>.json` にイベント詳細を集約（旧 `data.json` を `events/2026-zensanken-37.json` に移設）。
- `events.json`（一覧インデックス）を追加。`?id` 無しのルートURLは一覧表示。
- `sw.js` の precache を `data.json` → `events.json` に変更、`CACHE_VERSION` を v3 に。
- 目的: **過去のチラシ（QR）からアクセスしても当時の情報が永続的に見られる**ようにする。

### ドキュメント整備
- `CLAUDE.md`（プロジェクトガイド）と `progress.md`（本ファイル）を追加。

### デザインのモダン化
- カラーシステム整理: アクセント色を `brand`（indigo）として `tailwind.config` に定義。会場色を blue/emerald/amber/violet/rose に統一し、`COLOR` 辞書を `chip`/`dot`/`border` に再編。
- 絵文字を廃止し **SVGアイコン（Lucide系）** に置換（`ICONS` + `icon()` ヘルパ）。ボトムナビ・FAB・リンク・書籍・モーダルなど全面。
- UI刷新: グラデーションヘッダー、角丸カード＋ソフトシャドウ、タイムラインのドットレール表示、フェードイン演出（`prefers-reduced-motion` 対応）。

### PR #7 — ヘッダー/フッターのリデザイン（merged）
- テーマカラーを indigo → **オレンジ寄りの黄色（amber 系 `#f59e0b`）** に変更。`tailwind.config` の `brand` パレット1か所差し替えで全体へ波及。`<meta name="theme-color">`・`manifest.json` の `theme_color` も更新。
- ヘッダーを**ロゴ調**に変更。ロゴ文言は `eventInfo.logoMain`/`logoSub` で **JSON 駆動化**（未指定時は `title`/`subtitle` にフォールバック、`document.title` は正式名 `title` を維持）。
- **日付切替タブをヘッダーからタイムテーブル表示内（`view-program`）へ移動**。白背景向けに配色変更。
- フッターを **TIMETABLE / FILES / BOOKS の3タブ**に刷新（内部 `data-tab`・view ID は流用）。アクティブはピル形状（薄オレンジ背景＋濃オレンジ）、非アクティブはグレー。
- シェル変更に伴い `sw.js` の `CACHE_VERSION` を v5 に。

### PR #8〜#10 — ヘッダーの微調整（merged）
- **#8**: ヘッダー文字をセンター揃え。ヘッダー/フッターの最大幅を `max-w-3xl` → `max-w-2xl` に縮小・中央寄せ。ヘッダー下端を `rounded-b-2xl` に。
- **#9**: ヘッダーの幅をさらに縮小（`max-w-2xl` → `max-w-md`）。
- **#10**: ヘッダーの高さを縮小（縦パディング `pt-4 pb-3.5` → `pt-2.5 pb-2`、ロゴ `text-3xl` → `text-2xl`、サブ `text-sm` → `text-xs`）。
- 各 PR でシェル変更に合わせ `sw.js` の `CACHE_VERSION` を v6 → v7 → v8 へ順次更新。

### PR #11 — イベント毎ブランドカラー対応＋会場カラーの統一（open）
- **ブランド色のイベント毎指定**: `brand` パレットを **CSS 変数 `--brand` 基準の `color-mix()`** 化し、メイン1色から `brand-50〜800` を自動生成。`events/<id>.json` の `eventInfo.brandColor`（16進1色）を `applyBrandColor()` が実行時に `--brand` へ適用（`<meta name="theme-color">` も同期、不正値は既定 amber にフォールバック）。既定値は `<style>` の `:root { --brand: #f59e0b }`。
- ハードコードされていた `amber-*` アクセント（テーマバナー・お知らせ）を `brand-*` に統一。
- **会場カラーの統一**: 検証により、`tailwind.config` の `venue.*` パレット＋ `bg-venue-*` safelist が**未使用**で、描画する `COLOR` 辞書が safelist 外の標準色を参照していた不整合を発見。`COLOR` 辞書を `venue.*` へ接続（`blue→venue-blue` / `green→venue-green` / `orange→venue-coral` / `purple→venue-violet`）、未使用の `red` を削除。ブランド amber と C会場の色相衝突も解消。
- `safelist` に `brand-*` クラスを追加。`README.md` / `CLAUDE.md` に色設定の仕様を追記。`sw.js` の `CACHE_VERSION` を v9 に。

### 会場マップのイベント別命名対応
- 会場はイベント毎に異なるため、会場マップもイベント単位で差し替えられるよう運用を整理。**仕組み自体は既存**（`eventInfo.venue.mapImage` を `renderMaterials()` が読み込み。パス/外部URL両対応、未指定時は画像非表示で `mapNote` のみ）。
- 汎用名 `assets/venue-map.svg`（中身は筑波大附属小専用）を、アイコン（`icon-<id>.svg`）と同じイベント別命名 `assets/venue-map-2026-zensanken-37.svg` に **git mv でリネーム**。`events/2026-zensanken-37.json` の `mapImage` を新パスに更新。
- `CLAUDE.md`／`README.md`／`template/README.md` に「イベント別会場マップの命名・運用ルール」を追記。`index.html`・`sw.js` は不変（シェル変更なしのため `CACHE_VERSION` 更新不要）。

### 第27回 全国国語授業研究大会の追加
- Excel入力シート（`イベント情報入力シート2026-zenkokuken-27`）から `events/2026-zenkokuken-27.json` を新規生成（eventInfo／rooms／sessions 16本／notices／forms／books 7冊）。並行行（公開授業II・協議会のA/B会場、教材別ワークショップⅠ/Ⅱ各8本、テーマ別ワークショップ10本）は1セッションの `items[]` に集約。
- `events.json` に1エントリ追記（計2件）。実体マニフェスト `events/2026-zenkokuken-27.webmanifest` とブランドアイコン `assets/icon-2026-zenkokuken-27.svg`（背景 `#00a7d8`・文字「全国研」）を追加。
- 申込みリンクを本番URLに更新（東洋館オンラインショップ `products/27kokugojyugyouken`／Peatix `27kokugojyugyouken.peatix.com/view`）。
- 会場マップ画像はシート未指定のため `mapImage` 省略（`mapNote` のみ）。

### 一覧ページの東洋館カラー化＋カード個別色
- 一覧ページの基調色（`:root { --brand }` 既定値）を **東洋館イメージカラーのイエロー `#ffd900`** に変更。
- `events.json` の各エントリに `brandColor` を追加し、`renderEventList()` でカードごとに `card.style.setProperty("--brand", …)` を設定。`brand-*`（`color-mix` ベース）が再計算され、**カード単位でイベント固有色**（全国研＝シアン／全算研＝amber）に。シェル変更のため `sw.js` の `CACHE_VERSION` を v18→v19 に更新。

### 当日ナビゲーション（日付タブ自動選択＋現在時刻スクロール）
- **当日タブの既定表示**: `renderDateTabs()` の初期選択を「常に初日」から「今日と一致する `eventInfo.dates[].date`（`YYYY-MM-DD`）があればその日、なければ初日」に変更。判定は閲覧端末のローカル日付。ユーザーが手動でタブを切り替えれば `activeDateId` が優先される。
- **現在時刻へ自動スクロール**: `renderTimetable()` に、当日のみ「現在時刻 ≤ `start` を満たす最後のセッション（＝進行中／直近）」を判定し、`scrollIntoView({ block:"center" })` で自動スクロール＋対象カードに `ring-2 ring-brand-500` の枠を付与する処理を追加。当日以外・全枠が未開始の場合は何もしない（従来通り先頭表示）。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v19→v20 に更新。

### 全算研の会場配色調整
- rooms の濃淡入れ替え: 1音 `greenDeep`→`green`（淡）、2音 `green`→`greenDeep`（濃）。
- 会場マップSVG（`venue-map-2026-zensanken-37.svg`）の配色を TIMETABLE タブの会場色に統一（講堂＝濃青／1体＝淡青／1音＝淡緑／2音＝濃緑）。修正前は1体＝コーラル・1音＝バイオレットで不一致だった。`index.html`・`sw.js` は不変。

### 品質・アクセシビリティ改善 + リポジトリ監査
- **当日ライブUX**: 「現在」ハイライトが1分毎に自動追従（`updateNowMarker()` + `setInterval`、タブ復帰時は `visibilitychange` で即時更新）。従来は描画時に一度だけ計算され時間経過に追従しなかった。進行中セッションに「進行中」バッジ（パルスドット）を追加。自動スクロールは初回描画のみに変更（従来は日付タブ切替のたびに発火し操作を妨げていた）。当日タブに目印ドットを追加。
- **ローディング/オフラインUX**: 初期表示にスケルトンローディングを追加（従来は「読み込み中…」テキストのみ）。オフライン時のバナー表示を追加。
- **safe-area対応**: ヘッダー（`padding-top: env(safe-area-inset-top)`）・FAB（`env(safe-area-inset-bottom)`）がノッチ端末で欠けていたのを修正。
- **コンテナ幅統一**: ヘッダー`max-w-md`／メイン`max-w-3xl`／ナビ`max-w-2xl`のバラつきを `max-w-2xl` に統一。
- **アクセシビリティ**: ボトムナビに `aria-current`、日付タブに `aria-pressed`、モーダルに `role="dialog"`/`aria-modal` とフォーカス管理（開閉時のフォーカス移動・復帰）を追加。
- **書籍cover空白バグ**: `cover: " "`（空白のみ）が truthy 判定されて無駄な画像リクエスト→エラー→プレースホルダ差し替えが発生していたのを trim 判定で修正。`amazon_url` を `url` 未指定時のリンク先フォールバックとして活用（従来は取得のみでリンクに未使用）。
- **sw.js プリキャッシュ不整合の解消**: `APP_SHELL` が実在しない `assets/venue-map.svg`（毎回404）と特定イベントの `icon-2026-zensanken-37.svg` をハードコードしていたのを削除。イベント別アセットは Network First のオンデマンドキャッシュに委譲し、新イベント追加時に `sw.js` を触る必要をなくした。
- **キャッシュ名の汎用化**: `CACHE_NAME` が旧「算数フェス」由来の `sansu-fes-*` だったのを `t-pod-*` に変更（汎用シェル方針に合わせる）。activate 時に旧接頭辞も掃除。
- **データ清掃**: `events/2026-zenkokuken-27.json` books[0] に露出していた編集メモ（「主催／書籍著者としては未確認」等）を削除。両イベントの books の空白のみの `cover`/`amazon_url` を除去。
- **テンプレ導線の補完**: `template/make_template.py` の rooms 色選択肢に濃色 `blueDeep`/`greenDeep` が欠落していたのを、`index.html` の `COLOR` 辞書（6種）に揃えて追加。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v20→v21 に更新。

### 一覧ページの favicon を東洋館出版社サイトに統一
- `index.html` の `<link rel="icon">`/`<link rel="apple-touch-icon">`（静的既定値）を、`www.toyokan.co.jp` と同一の画像（`logo_icon_....jpg`、CDN直接参照）に変更。
- イベントページの `applyAppIdentity()` によるアイコン実行時生成（PWAホーム画面追加用）は影響を受けず従来通り動作（セレクタで `link[rel="icon"]` を取得して上書きするため）。
- `assets/favicon.svg` はコードから参照されなくなったため `sw.js` の `APP_SHELL` プリキャッシュ対象からも除外（ファイル自体は予備として残置）。`CACHE_VERSION` は v21 のまま（未デプロイのため据え置き）。

### オール筑波 算数サマーフェスティバル2026の追加
- Excel入力シート（`イベント情報入力シート_入力済み_2026-math-summer-fes_関連書籍補完.xlsx`）から `events/2026-math-summer-fes.json` を新規生成（eventInfo／sessions 15本／notices 6件／forms／books 14冊）。並行セッションが無いため `rooms: []`。会場マップ画像はシート未指定のため `mapImage` 省略（`mapNote` のみ）。
- `events.json` に1エントリ追記（計3件）。`manifestPath`（実体マニフェスト）は未作成。

### PWA アイコン文字をヘッダー表示と分離可能に（`iconLabel` 追加）
- 従来はホーム画面追加時のアプリ名（ヘッダー表示）とアイコン文字が同一の `logoMain` から派生し、アイコンは `logoMain` 先頭3文字固定だった。「ヘッダーは正式名、アイコンは短い略称にしたい」（例: ヘッダー「算数サマー2026」／アイコン「算サマ」）というニーズに対応するため、`eventInfo.iconLabel`（任意）を新設。
- `index.html` の `applyAppIdentity()` を `info.iconLabel || info.logoMain` でアイコン生成するよう変更（`iconLabel` 未指定時は従来通り `logoMain` 先頭3文字なので既存イベントは無影響）。汎用ロジックのため `template/README.md`／`README.md` にも仕様を追記。
- `events/2026-math-summer-fes.json` に適用（`logoMain`＝「算数サマー2026」、`iconLabel`＝「算サマ」）。シェル変更のため `sw.js` の `CACHE_VERSION` を v22→v23 に更新。

### デザイン検証と改善（コントラスト自動判定・縮小ヘッダー・LINE導線見直し）
- **背景色に応じた文字色の自動切替（コントラスト事故の解消）**: 一覧ページの基調色 `#ffd900`（純黄色）の上にヘッダー等が白文字で載り、WCAG 大幅未達で判読困難だった。CSS 変数 `--brand-fg` を新設し、ブランド色の sRGB 相対輝度（`relativeLuminance()`）を JS で判定して、明るい色なら濃紺 `#1e293b`・暗い色なら白を自動選択（`updateBrandFg()`、閾値 0.55）。ヘッダー・FAB・日付タブ active・インストールバナー・実行時生成する PWA アイコン文字の固定 `text-white` を `text-brand-fg` に置換。`tailwind.config` の `brand.fg` と `safelist` に追加。どんな `brandColor`（明暗どちらでも）でも文字が読めるようにした。`<meta name="theme-color">` の既定値も `#f59e0b` → `#ffd900` に是正（`--brand` 既定と一致）。
- **スクロール連動の縮小ヘッダー＋質感更新**: 濃いグラデ＋`drop-shadow`＋`shadow-lg` の 2018 年頃風マテリアルを、フラット寄り（`shadow-sm`・グラデ控えめ）に更新。スクロール（>48px）でタイトル縮小＋サブタイトル非表示にコンパクト化し、上端付近（<8px）で復元（閾値を分けてちらつき防止、`transition` は `font-size`/`padding` のみ 150ms、`prefers-reduced-motion` では即時切替）。縦空間を確保。
- **LINE 友だち追加ポップアップの表示条件を再設計**:
  - 従来: イベント初回訪問の 0.9 秒後に全画面表示（コンテンツ閲覧前の割り込みで離脱要因）。
  - 変更後（「閲覧後」表示・1回のみ・`localStorage` 制御）:
    - **イベントページはオプトイン**（`eventInfo.linePromo === true` のときだけ表示）。現行3イベントはすべて LINE 経由でアクセスするため既定の非表示のまま（`linePromo: false`）。新規イベントは `true` を指定すれば表示できる。
    - **一覧ページ（`?id` 無し）は初回訪問時のみ表示**（LINE 以外からの流入もあるため）。縦が短くタブ切替も無いので、スクロール 120px 超または 5 秒経過で表示（イベントは 300px／30秒）。
  - 表示タイミングは「一定スクロール・タブ切替・一定時間経過のいずれか早いタイミング」。モーダル表示中は重ねず閉じてから表示。
- **テンプレート更新**: `template/make_template.py` の基本情報シートに「LINE追加ポップアップ（true/false）」行を追加し `イベント情報入力シート.xlsx` を再生成。`template/README.md` に `linePromo` のオプトイン仕様を追記。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v23→v24→v25 に更新（コントラスト/ヘッダー対応で v24、LINE 導線見直しで v25）。

### ノッチ/ステータスバー色をイベント色に揃える（`theme-color` のパース時反映）
- **課題**: LINE 等のアプリ内ブラウザは `<meta name="theme-color">` を**ページのパース時に一度だけ**読み、JS の事後更新を反映しない。従来 `applyBrandColor()` は JSON 取得後にシアン等へ更新していたが、LINE のバー（ノッチ/ステータスバー部）は静的既定値 `#ffd900`（黄）で固定されたままだった。
- **対応（汎用シェルを維持したまま生成的に解決）**: イベントのブランド色を **localStorage にキャッシュ（キー `t-pod:theme:<id>`）** し、次回訪問時に **head の inline スクリプトがパース時点で `theme-color` へ即時反映**する方式に変更。
  - `applyBrandColor()`: 適用したブランド色を同キーにキャッシュ（次回用）。
  - head inline スクリプト: `?id` に対応するキャッシュ色があればパース時点で `theme-color` メタへ反映。`theme-color` メタを inline スクリプトより前に移動（スクリプトから参照可能に）。
- **既知の制約**: 2 回目以降の訪問は確実に反映。まっさらな端末の**初回訪問のみ**、色を知る前に LINE がバーを読むため一瞬既定の黄になり得る（イベントアプリは LINE から繰り返し開く運用のため実用上ほぼ問題なし）。初回も完全一致させるにはイベント別の実体 HTML か色の静的埋め込みが必要で、「単一汎用シェル」方針とのトレードオフ。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v25→v26 に更新。

### ノッチ色の恒久対策（Service Worker がシェル HTML の theme-color を書き換え）
- **残っていた問題**: 上記の localStorage + JS 方式でも、iOS Safari は**サーバーから受け取った生 HTML の静的 `theme-color`（既定 `#ffd900`）**をパース〜リロード時のオーバースクロール背景に採用し、JS の事後変更の反映が遅れる。結果、**リロード直後にノッチ/上部が黄色へ戻る**現象が残っていた（読み込みスピナー中は特に顕著）。
- **恒久対策**: Service Worker の fetch ハンドラで、**ナビゲーション要求（`request.mode === "navigate"`）を専用処理**（`handleNavigate()`）に分離。`?id` があれば `events/<id>.json` の `eventInfo.brandColor`（`getBrandColor()`、キャッシュ優先→無ければ取得）を並行取得し、シェル HTML 内の `<meta name="theme-color" content="…">` を**イベント色へ書き換えてから返す**。これにより **Safari が受け取る生 HTML の時点で theme-color が正しい色**になり、パース/リロード時も黄へ戻らない。一覧ページ（`?id` 無し）や取得失敗時は素のシェル（既定 `#ffd900`）を返す。
  - 従来の Network First は `networkFirst()` に切り出して共通化。`HEX_RE` で色を検証。
  - **検証**: スクリプト無効（`sandbox="allow-same-origin"`）の iframe で読み込み（＝ページ JS が一切動かない状態）、SW 書き換え結果のみの `theme-color` が 全国研`#00a7d8`／全算研`#f59e0b`／サマー`#0ea5e9`／一覧`#ffd900` と各イベント色に一致することを確認。
  - 効果は SW 制御下（＝2 回目以降、実質すべてのリロード）で有効。SW 未登録の初回ヒットのみ生の既定色。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v26→v27 に更新。

### 初回訪問のノッチ色も揃える（SW 制御取得後の自動リロード）
- **残っていた問題**: 上の SW 書き換えは **SW が制御している場合のみ**有効。新しい端末の**初回アクセス**は SW 未制御のため GitHub Pages の生 HTML（既定 `theme-color`=黄）がそのまま表示され、iOS Safari はそれをパース時に読んでノッチが黄のまま固定される（本番 v27 で実機確認 → 初回は黄のまま、を再現）。
- **対応**: `registerSW()` を変更。**初回訪問（`navigator.serviceWorker.controller` が null）でイベントページを開いたときだけ**、SW が制御を取得した瞬間（`controllerchange`）に **一度だけ `location.reload()`**。リロード後のナビゲーションは SW 制御下で `handleNavigate()` が書き換えた HTML を返すため、ノッチ色が確定する。
  - **ループ防止**: `sessionStorage["t-pod:sw-reload"]` で「セッション内1回のみ」に制限。制御下で開始した2回目以降・一覧ページ・更新時は自動リロードしない（`wasUncontrolled && getEventId()` 条件）。
  - **検証**: SW 全解除＋キャッシュ削除で新端末を再現 → イベントページを開くと自動で1回リロードし、`sessionStorage` フラグが立ち、SW 制御下＋正しい色（全算研 `#f59e0b`）になることを確認。2回目ナビ・手動リロードでは再リロードしない（ループ無し）ことも確認。コンソールエラー無し。
  - **トレードオフ**: 新端末の初回のみ、黄の一瞬 →（〜1秒）自動リロード → イベント色、という遷移になる（LINE/QR から繰り返し開く運用では実用上問題なし）。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v27→v28 に更新。

### 自動リロードを別 URL 置換に変更（iOS Safari の status bar 色キャッシュ対策）
- **問題**: 本番 v28 でも iOS Safari では最上部（status bar/ノッチ帯）が黄のまま。この帯は iOS Safari が `theme-color` で塗る領域で、**同一 URL のリロードでは iOS Safari が status bar の `theme-color` を再読込しない**（初回パース値を保持する）ことが原因と推定。SW 書き換え自体は本番でも有効（Chrome の sandboxed iframe で amber/シアンを確認済み）だが、初回リロード後も iOS がキャッシュした黄を保持していた。
- **対応**: `registerSW()` の自動リロードを、同一 URL の `location.reload()` から**キャッシュバスター付きの別 URL への `location.replace()`**（`?id=… &_r=1`）に変更。別 URL にすることで iOS Safari に確実に再パースさせ、SW 制御下で書き換わった `theme-color`（イベント色）を読ませる。`?id` は保持されるので SW の `handleNavigate()` はそのまま機能。
  - **ループ防止**: URL に `_r` があれば「リロード済み」とみなし再リダイレクトしない（＋従来の `sessionStorage["t-pod:sw-reload"]` ガードも併用）。
  - **検証（Chrome）**: 新端末再現（SW 全解除＋キャッシュ削除）→ イベントページを開くと `?id=…&_r=1` へ自動置換され、SW 制御下＋正しい色（全国研 `#00a7d8`）に。`_r` 付き URL では再リダイレクトせず（`_r` は1個のまま）ループ無し。コンソールエラー無し。
  - **未検証事項（要実機確認）**: iOS Safari が `theme-color` を **URL 単位でキャッシュ**するなら別 URL 化で直る想定。もし **タブ単位でキャッシュ**する挙動なら本対応でも直らず、その場合はイベント別の静的 HTML エントリ（生成物、URL/QR 方針の見直しを伴う）が必要になる。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v28→v29 に更新。
- **結果・現状（2026-07-10 時点）**: v29 をデプロイして iOS Safari 実機で確認したが **ノッチ帯は黄のまま＝反映されず**。「URL 単位キャッシュ」仮説では直らなかったことになり、この件は**いったん保留**とする。
  - **制約（今後の方針）**: **公開済み URL（`?id=<id>`）は変更しない**方針を確定。したがって `&_r=1` を付与する現行 v29 の別 URL 置換は暫定であり、恒久策としては採らない（URL が汚れる／共有・ブックマーク・履歴に残るため）。URL を変えずにノッチ色を合わせる方法を引き続き検討する。
  - **次に検討する案（URL 不変が前提）**: ①`?id=<id>` のまま Service Worker 側で初回ナビゲーションを確実に横取りする（`clients.claim()` のタイミング／`navigationPreload` 等の見直し）。②`&_r=1` を付けず `history.replaceState` で URL を戻しつつ再パースさせる方法の可否検証。③イベント別の静的 HTML を用意しても**公開 URL は `?id=` のまま**にできる配信（例: リダイレクトではなく SW が `?id=` に対してイベント別 HTML を合成して返す）。いずれも iOS 実機での検証が必須。

### URL 不変のノッチ色対策（セーフエリア上端をブランド色で描画）
- **方針転換**: iOS Safari 実機では、`theme-color` を SW で書き換えても同一タブのノッチ/ステータスバー色が黄のまま保持されるケースが残ったため、`theme-color` の再読込に依存しない対策へ変更。
- **対応**:
  - `registerSW()` から `&_r=1` 付き `location.replace()` を撤去。公開済み URL（`?id=<id>`）を汚さない方針に戻した。
  - 画面最上部の `env(safe-area-inset-top)` 領域を `--brand` で塗る固定レイヤー `.safe-area-top-bg` を追加。ヘッダー本体は `max-w-2xl`・角丸のまま、ノッチ帯だけは画面端までイベント色で埋める。
  - `apple-mobile-web-app-status-bar-style` を `black-translucent` に変更し、ホーム画面追加時にも上端の Web コンテンツ色がステータスバー背面に出やすい設定にした。
- **効果の狙い**: iOS が `theme-color` をタブ単位で保持しても、実際に表示されるセーフエリア背景をアプリ側の CSS 変数で制御する。`applyBrandColor()` が `--brand` を更新するため、イベント JSON 読み込み後にノッチ帯も個別研究会ページの色へ揃う。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v29→v30 に更新。
- **結果（2026-07-10、v30 デプロイ後 iOS Safari 実機で確認）**: **解消を確認**。`theme-color` の再読込に依存せず、セーフエリア上端を独自レイヤーで塗る方式に切り替えたことで、個別イベントページを開くとノッチ/ステータスバー帯がそのイベントのブランド色になった。公開済み URL（`?id=<id>`）も変更なし。長らく残っていたノッチ色問題はこれで**解決とする**。

### お知らせの未読バッジ化（1度閲覧すると赤い数字が消える）
- **課題**: ヘッダーのベルの赤いバッジが `notices.length`（総件数）を無条件に表示し続け、既読状態を持っていなかった。一度お知らせを読んでも数字が消えず、常時点灯したバッジは通知としての意味を失っていた。
- **対応（案B: 内容ベースの既読判定）**: 単純な「開いたら以後非表示」ではなく、**未読件数**を表示する方式を採用。会期中に主催者がお知らせを追加・修正しても参加者が見落とさないようにするため。
  - 既読は各お知らせの**署名（`title` + `body`）の配列**を `localStorage["t-pod:notices-read:<イベントid>"]` に保存（イベント毎に独立）。件数ではなく内容で判定するため、お知らせが追加・更新されるとその項目だけが未読に戻り、再びバッジが出る。
  - `renderNoticeBadge()`: 未読 = 保存済み署名に含まれないお知らせ、として件数を算出。0 件ならバッジ自体を非表示（従来は `hidden` を外すだけで、消す経路が無かった）。
  - `openNotices()`: モーダルを開いた直後に `markNoticesRead()` → `renderNoticeBadge()` で即座にバッジへ反映。
  - localStorage が使えない環境（プライベートモード等）は try/catch で握って「全件未読」として振る舞い、表示は壊れない（既存の LINE/インストール導線の localStorage 利用と同じ流儀）。
- **検証**: メインスクリプトの構文チェック（`node --check`）と `git diff` を確認。ブラウザ実機での挙動確認は未実施。
- **付随**: Edit ツールが `index.html`/`sw.js` を CRLF に一括変換してしまったため LF へ復元（差分が全行に及んでいないことを確認済み）。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v33→v34 に更新。

### ボトムナビのピル幅統一＋UI不揃い解消
- **ボトムナビ選択時ピルの幅揃え**: `.navpill` 3箇所（TIMETABLE / FILES / BOOKS）に `w-full max-w-[120px]` を追加。ラベル長がバラバラなため選択するたびに色面の幅が変わっていたのを、3つとも常時 120px で固定。rem 指定では環境次第で字が折り返す恐れがあったため **px 固定**に。
- **選択時の `font-extrabold` トグルを廃止**: 選択時だけ字が太くなり文字幅が微動していたのを防止。強調は `grad-pill`（背景色）+ `shadow-fab` で充分効く。`safelist` の `font-extrabold` は日付タブが使うため**残す**。
- **アイコンタイルの寸法統一**: 追加バナーのアイコン枠 `w-9 h-9 rounded-lg` → `w-10 h-10 rounded-xl`（資料リンクのアイコン枠と同寸に）。
- **バナー角丸の統一**: オフラインバナー・エラーボックスの `rounded-lg` → `rounded-xl`（追加バナーと同じに統一。カード `rounded-2xl` より一段小さく階層は保つ）。
- **検証済み**: 3タブピル幅・高さ一致、切替時にラベル位置が動かない、320px/375px 幅でも 3つ同時に縮小・はみ出しなし。アイコンタイルと角丸も意図通り統一。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v34→v35 に更新。

### クレジット表記の追加＋ヘッダー2行目・テーマ表示の改善
- **クレジット表記**: アプリの開発・運営元（東洋館出版社）を全タブ・一覧ページの本文最下部に表示。当初は FILES タブと一覧に「APP CREDITS」見出し付きで入れたが、目立ちすぎたため段階的に簡素化。最終形は `<main>` 末尾に1要素だけ置く方式（`App by TOYOKAN PUBLISHING CO., LTD.`、9px・`text-slate-300`・区切り線なし）。タブはセクションの表示/非表示切替なので、1箇所で TIMETABLE / FILES / BOOKS と一覧ページの全画面に出る（4箇所重複を回避）。
- **ヘッダー2行目を正式名称に**: `$("eventSubtitle")` の優先順位を `info.title || info.logoSub || info.subtitle` に変更。従来は `logoSub`（例「全国算数授業研究大会2026」）が出ていたのを、`title`（例「第37回 全国算数授業研究大会」）優先に。`logoSub` はフォールバックとして残す。
- **テーマのサブテーマ改行**: `splitTheme()` ヘルパを追加。`――`/`―`/`—`/`〜` の最初の区切りでメイン／サブに分割し、区切り記号ごとサブテーマを `<br>` で2行目に送る。文字サイズは1行目と同じ、`#themeText` を `inline-block` にして2行目の行頭を1行目（「テーマ」ラベル右）に揃える。区切り記号は原文どおり残す。テーマバナーは2行でも収まるよう `rounded-full` → `rounded-2xl`。描画は `escapeHtml()` を通す。
- **データ修正**: 一覧ページは `events.json` を読むが、`2026-math-summer-fes` のエントリだけ `theme` がサブテーマ抜き（末尾も半角 `?`）だったため、個別 JSON と同じ全文へ揃えた（一覧でサブテーマが表示されない不具合の修正）。
- **検証**: ローカルサーバ＋ブラウザ実機で全3イベント・一覧ページ・全タブを確認。コンソールエラーなし。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v35→v36 に更新。

### 概要カードの主催表記（`organizer` ほか）＋PWA表示名の分離（`appName`）
- **主催・共催・協力・後援**: 概要・お知らせの1つ目のカード（会場名の下）に表示する `eventInfo.organizer` / `coOrganizer` / `cooperation` / `support` を新設（すべて任意）。役割ごとに `if` を並べず `ORG_ROLES`（キー→ラベルの表）を回す `orgRows()` に集約したので、「協賛」等が増えても1行追加で済む。**指定があるものだけ行が出る**（未指定・空文字・空配列は非表示）。値は文字列でも配列でも可で、配列は「／」区切りで1行にまとめる。長い団体名は折り返すが `items-start` + `flex-shrink-0` でアイコンが潰れず行頭が揃う。アイコンは Lucide の `users` を `ICONS` に追加。
- **データ**: 3イベントに `organizer` を設定（全算研＝全国算数授業研究会／全国研＝全国国語授業研究会／算数サマー＝筑波大学附属小学校算数部）。共催・協力・後援は各大会の実情報が未確認のため未設定（推測で入れない）。
- **PWA表示名の分離（`appName`）**: 従来ホーム画面のアプリ名は `logoMain` 固定で、ヘッダー1行目と同じ文字列しか使えなかった。「ヘッダーは『算数サマー2026』のまま、ホーム画面は『算サマ2026』にしたい」というニーズに対応するため `eventInfo.appName`（任意）を新設。`applyAppIdentity()` で `document.title`（タブ名）は `logoMain` を維持し、**マニフェストの `name`/`short_name` と iOS の `apple-mobile-web-app-title` だけ `appName` を使う**。`events/2026-math-summer-fes.json` に `"appName": "算サマ2026"` を適用（同イベントは `manifestPath` 無しの実行時生成マニフェストのため反映される。実体マニフェストを持つ全算研・全国研は従来どおりで無影響）。
- **クレジット微調整**: 文字 9px→10px、上余白 `mt-8`→`mt-6`（ほんの少し大きく・少し上へ）。
- **ドキュメント/テンプレート**: `README.md`（Features・`events.json`/`events/<id>.json` スキーマ）、`template/README.md`（変換仕様）、`template/make_template.py` の基本情報シートに「主催／共催／協力／後援」「PWA表示名」行を追加し `イベント情報入力シート.xlsx` を再生成。
- **検証**: ローカルサーバ＋ブラウザ実機で3イベントを確認（主催行の表示、全ロール・複数団体・未指定・空文字/空配列の各ケース、PWA名が算サマ2026／ヘッダー・タブ名は算数サマー2026）。コンソールエラーなし。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v36→v37 に更新。

### スワイプでのタブ移動＋すりガラス UI＋軽量アニメーション
- **左右スワイプでタブ移動**: スマホで TIMETABLE / FILES / BOOKS を横スワイプ切替できるようにした（従来はボトムナビのタップのみ）。**指追従のライブドラッグは採用しない**。追従には `preventDefault()` が必要でタッチリスナが非 passive になり、縦スクロールのカクつきを招くため。代わりに「軸ロック＋離した時点でコミット」方式とし、`touchstart`/`touchmove`/`touchend` すべて `{ passive: true }` のまま実現した。
  - 判定: 初回移動で軸を決め（`|dx| > |dy| * 1.4` のときだけ横スワイプ）、`touchend` で `|dx| >= 60px` かつ 600ms 以内なら隣タブへ。閾値未満・縦優位のジェスチャは無視。
  - 無効化条件: 一覧モード（`#bottomNav` が hidden）／モーダル・LINE ポップアップ表示中（`body.modal-open`）／`.scroll-x`（横スクロールする会場列）・`a`・`button` の上／両端でのさらに外向き（ループしない）。
  - タブ順は `TAB_ORDER` 定数に集約し、`setTab()` と共用。
- **タブ切替のアニメーション**: `setTab(tab)` を移動方向つきに拡張。`hidden` トグルは維持したまま、表示するビューへ `view-in-right` / `view-in-left`（`translate3d` + `opacity` の 220ms）を付け直す（クラス除去→reflow→再付与で再生を保証）。方向は現タブ→新タブの index 差から自動決定するため、スワイプでもナビタップでも一貫した向きに動く。
- **アクティブ・インジケータのスライド**: ボトムナビ・日付タブとも、選択中の要素にクラスを付け替える方式から、**絶対配置のインジケータ1枚を `translate3d(index*100%)` で動かす**方式に変更（`.tab-indicator`）。
  - これに伴い `renderDateTabs()` を分割: DOM 構築は初回のみ、切替は `setActiveDate(id)` がクラスとインジケータだけを動かす。従来はクリックのたびに `innerHTML = ""` から全再生成しており、アニメーションが原理的に入らないうえ描画コストも無駄だった。
  - `#dateTabs` の `gap-1` を**削除**。flex の gap があるとボタン幅と `100%/n` がずれ、インジケータが1ボタン分ちょうど移動しない（実測で約2pxずれる）。gap を無くすことで幅と移動量が厳密に一致する（1〜4日イベントで位置ズレ 0.03px 未満を確認）。
- **すりガラス調**: `#bottomNav` を `bg-white/75 backdrop-blur-xl`、`#dateTabs` を `bg-slate-200/70 backdrop-blur-md` に。`@supports not (backdrop-filter: …)` で非対応環境は不透明塗りへフォールバック。
- **日付タブの選択・非選択の色分け（コントラスト実測にもとづく改訂）**: 当初トラックを `bg-white/60` にしたところ「白ピル on ほぼ白」となり選択が判別できなくなったため、地をグレー（`bg-slate-200/70`）に戻した。
  - 検討の過程で**ブランド色ベタ塗りのピル＋白文字**を試作したが、実測で **1.53〜4.42:1**（グラデーションの全域で AA 未達）と判明し不採用。白文字を AA に通すにはブランド色を 50% まで暗くする必要があり、ピルが暗い茶色になってブランドの明るさが失われる。
  - 採用形: **選択＝白ピル＋ブランド濃色文字の太字**、**非選択＝グレー地に `slate-600`**（6.45:1）。「ブランド色を濃く使う」既存パターン（カテゴリチップの `text-brand-700 bg-brand-50`）の流用で、ブランド識別性と可読性を両立。
  - ※ 濃色の配合はこの時点で `--brand 45%` としていたが、後述「ボトムナビの選択タブのコントラスト解消」で **48%（`brand-800` 相当）に統一**した（本文中で `brand-700` と同配合としていたのは誤り。`tailwind.config` の `brand-700` は実際には 62% で、既定の黄では 3.45:1 と AA 未達）。
  - 色の出し分けは JS のクラス付け替えをやめ、`aria-pressed` に紐づく CSS へ寄せた（状態の源を1つに保つ）。
- **モーダルの開閉アニメ修正**: パネルの `animate-in` は静的付与のため**初回しか再生されていなかった**（2回目以降は無演出）。`replayIn()` で再付与し、閉じるときは `animateOut()`（`animate-out` + バックドロップのフェード、`animationend` 到達不能に備え 200ms のフォールバック）を経てから `hidden` を付ける。`#modal` と `#linePopup` で共用。`prefers-reduced-motion` 時は即閉じ。
- **リスト項目のスタッガー上限**: タイムテーブル行の `animationDelay` が `idx * 30ms` と青天井で、セッションの多い日は最終行が 900ms 遅れて出ていた。`Math.min(idx, 5)` で**先頭6件で頭打ち**に。
- **`prefers-reduced-motion`**: 既存ブロックに新規クラス（`view-in-*` / `animate-out` / `.tab-indicator` / `.navpill` / バックドロップ）を追加。スワイプ自体は移動手段であり演出ではないため残す。
- **検証**: ローカルサーバ＋ブラウザで、合成タッチイベントによりスワイプ全分岐（左右各2段・両端で停止・30px は無視・縦優位は無視・`.scroll-x`／ボタン上は無視・一覧モードは無効）を確認。インジケータ位置は 1/2/3/4 日イベントでズレ 0.03px 未満、ボトムナビは 0.01px 未満。モーダルは2回以上の開閉で毎回再生。コントラストは実測値で確認。コンソールエラーなし。
  - **未検証**: `prefers-reduced-motion` の OS 設定を切り替えた実動作、iOS 実機（セーフエリア・PWA 起動時のすりガラス表示）。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v37→v38 に更新。

### 登壇者の氏名・所属の確認（残課題の消化）＋ボトムナビのコントラスト解消
- **氏名・所属の照合（`events/2026-zensanken-37.json`）**: 画像から転記して判読が不確かだった箇所を、主催者提供の**役員名簿（会長・常任理事・全国理事・総務幹事・常任幹事）とチラシ本文**を正として全件突き合わせ、10 件を修正した。名簿を照合表にしてスクリプトで機械的に検出したので、目視では見落としやすい別字（髙/高・崎/﨑・裕/祐）も拾えている。
  - 氏名の別字: 尾崎正彦→**尾﨑正彦**（講演者。※`尾崎伸宏` は別人で 崎 のまま）／西村裕太→**西村祐太**／髙井淳史→**高井淳史**（※`髙橋正英` は名簿どおり 髙 のまま）／髙木美和→**高木美和**。
  - 所属: 向井大嗣（田辺市立会津小→**田辺第三小**）／永井伸行（関西国際学園**初等部→中高等部**）／河内麻衣子（豊島区立**高南朋有小→朋有小**）／藤本邦昭（熊本市**教育センター→教育委員会**）。
  - ワークショップのタイトル: 「**選び**ながら学びを深める」→「**遊び**ながら学びを深める」（意味が変わる誤り）／「新たな問いを**生み出す**子どもの目を育む」→「新たな問いを**生む**〜」。
  - 旧 TODO にあった「山本良一／島根・雲南市立木次小」「久保田健功」は既に正しい表記（山本聡一／兵庫・豊岡市立八条小、久保田健祐）になっており、対応不要だった。
  - **保留（要確認）**: ①`二瓶亮` の所属を JSON は「新潟大学附属新潟小」、名簿は「新潟県新潟大学附属新潟小」としている。国立大学附属校に県名を冠するのは不自然で名簿側の誤植と判断し、変更していない。②チラシのワークショップ欄は私立・国立校にも都道府県を付す表記（例「東京・筑波大学附属小」）だが、JSON と名簿は公立のみ都道府県を付す表記で統一されている。アプリ内の他セッションとの一貫性を優先し、都道府県は追加していない。
- **ボトムナビの選択タブのコントラスト解消（残課題を消化）**: `.grad-pill` 上の白文字が 1.2〜4.4:1 で AA 未達だった件に、案②（日付タブと同じ「淡地＋ブランド濃色文字」）を適用。
  - **検証で判明した設計上の制約**: ナビの地は白（`bg-white/75`）のため、**淡地ピル（`brand-50`〜`200`）も枠線（`ring-brand-200`〜`400`）も 1.0〜1.7:1 でほぼ見えない**（ブランド色が明るいと、その淡色は白と区別できない）。ベタ塗り `brand-500` ＋濃色文字も 3.5〜3.97:1 で不足。→ **日付タブが成立していたのは「グレーのトラック＋白ピル」だから**と判明したため、ボトムナビにも同じセグメンテッドコントロール構造（`bg-slate-200/60` のトラック＋白ピル）を導入した。
  - 濃色文字の配合を **`--brand` 48%（＝`brand-800` 相当）に統一**（日付タブの 45% も合わせた）。`brand-700`（62%）は既定の黄で 3.45:1 と AA 未達のため不採用。色指定は日付タブとボトムナビで 1 つの CSS ルールに集約。
  - 非選択も `slate-400`（**2.5:1・AA 未達**）から `slate-600`（約 6.9:1）へ。選択時の `font-extrabold` トグルは従来どおり入れない（文字幅が動くため。PR「ボトムナビのピル幅統一」の方針を維持）。
  - **実測（canvas で描画色を取得）**: 選択タブ文字は 既定の黄 5.52:1／amber 7.48:1／シアン 8.95:1／算数サマー 8.89:1 と**全テーマ色で AA 通過**。非選択 6.69:1。
  - **検証**: 白ピルとラベルの位置ズレ 0.03px 以内、320/375/430px でピル幅＝インジケータ幅が一致・はみ出しと折り返しなし、スワイプ全分岐に回帰なし。
  - **測定時の注意（ハマりどころ）**: ①`color-mix()` の計算結果を `getComputedStyle().color` で読むと Chrome は `oklab()` 表記で返すことがあり、`rgb()` 前提のパースだと誤った値になる（canvas に塗って `getImageData` で取るのが確実）。②`.navpill` に `color` の transition があるため、**バックグラウンドタブでは transition が進まず旧色のまま読める**。測定前に `transition: none` にする。
- シェル変更のため `sw.js` の `CACHE_VERSION` を v38→v39 に更新。

## 残課題 / TODO

### 2026-07-15 公開連絡先の整理

- `events/2026-zenkokuken-27.json`から電話番号、個人Gmail、担当者名・個人宛メールを削除。
- 問い合わせ先は研究会事務局と東洋館出版社の公式窓口を案内する表現へ統一。
- Tailwind Play CDNを`3.4.17`へ固定し、Service Workerのキャッシュをv33へ更新。

- [x] **GitHub Pages の有効化**（Settings → Pages → main / root。CNAME 設置済み・公開中）。
- [x] **判読困難だった氏名・所属の確認**（役員名簿と照合し10件修正。→「登壇者の氏名・所属の確認」の節）。ただし以下2点は**主催者への確認待ち**:
  - `二瓶亮` の所属: JSON「新潟大学附属新潟小」／名簿「新潟県新潟大学附属新潟小」。名簿側の誤植と判断して未変更。
  - ワークショップ欄の所属表記に都道府県を付すか（チラシは私立・国立にも付す／JSON・名簿は公立のみ）。現状は JSON の表記で統一。
- [x] **ボトムナビの選択タブ**（ブランド背景＋白文字へ変更。色の自動補正は行わず、イベントで指定したブランド色を使用）。
- [x] **`--brand-fg` の検討**: 自動補正案を一度実装したが、処理と色変化を避けるため撤回。ヘッダー、`grad-pill`、進行中表示、ジャンプボタン、インストール導線、書籍販売CTA、ボトムナビは指定ブランド色を直接使う。非アクティブ `cat-chip` の `brand-800` 化は維持。
  - 対象: `#jumpNow`（「▼ いまの予定へ」）／進行中セッションのプログレスバー・`cat-chip` のアクティブ状態（`grad-pill` に切替）／インストールバナーのカード（`from-brand-500 to-brand-600` に `text-brand-fg`）／ヘッダー（`#appHeader` のグラデ上の文字）。
  - 根本原因: `updateBrandFg()` が輝度閾値 0.55 で白／濃紺を切り替えており、amber・黄でも「白」を選ぶ。
  - **最終方針**: `--brand` を識別色・塗りの双方へ直接使用する。以下は自動補正を検討した際の測定記録として残す。
  - **着手前に判明している事実（実測済み・再計測不要）**:
    - **現状の `grad-pill` は幅が広すぎる**（`brand 55%+白` → `brand` → `brand 68%+黒`）。停止点の明暗差が大きすぎて、**白でも濃紺でも全域 4.5:1 を満たせない**（黄のみ濃紺で 4.87:1 ✅、amber 3.32／シアン 2.65／サマー 2.67 はいずれも ❌）。**どの案を採るにせよグラデ幅を狭めるのが前提**。
    - **案A（色はそのまま・文字色を実コントラストで選ぶ）**: グラデを `brand±8%` に狭めた場合、4色すべてで「濃紺」が選ばれる（黄 8.88／amber 5.78／サマー 4.51 ✅、**シアン 4.47 ❌** と僅差で不足）。白文字→濃紺への反転になり全イベントで見た目の変化が大きい。
    - **案B（文字は白のまま・塗りを自動で暗くする）**: 白文字 4.5:1 に必要な暗さは 黄 54.3%／amber 67.2%／シアン 77.0%／サマー 76.6%（`color-mix(brand X%, black)` 換算）。**シアン・サマーは 77% で色味がほぼ保たれる**が、**黄・amber は茶／オリーブ寄りになり印象が変わる**。二分探索で自動算出できるので、どんな `brandColor` でも破綻しない。
    - グラデを「`--brand-solid` から**暗い方向にだけ**」振れば、最も明るい停止点＝`--brand-solid` 自身となり全域で AA が保証される（明るい方向へ振ると破綻する）。
  - **残る判断**: 案B を採るとき、黄・amber のヘッダーが暗くなることを許容するか（＝ブランドの明るさ vs 白文字の一貫性）。許容しないなら、明るい色は入稿時点で弾く「ブランド色の選択制限」に倒す。
  - 文字サイズ別のしきい値も考慮の余地あり（`#eventTitle` は 27px 太字＝大きな文字なので 3:1 で可。`#eventSubtitle` 12px 等は 4.5:1 必要）。
  - なお `cat-chip` の非アクティブ（`text-brand-700 bg-brand-50`）は **`brand-700`＝62%** のため、既定の黄のような明るい色を `brandColor` に指定したイベントでは 3.45:1 になる（現行3イベントの色では 4.8:1 以上で通る）。①②いずれの対応でも合わせて見直したい。
- [ ] （任意）QRコード生成の仕組み。
- [ ] （任意）書籍データの拡充（実際の販売書籍リスト）。
- [ ] （任意）会場マップ画像（`assets/venue-map-2026-zensanken-37.svg`）の本番図面への差し替え。
- [ ] （任意）PWAアイコン（`assets/icon.svg`）の本番デザインへの差し替え。

### 2026-07-16 スワイプ追従・ボトムナビ強調

- タブの左右スワイプを、指を離してから切り替える方式から、横軸ロック後に現在ビューが指へ追従する方式へ変更。縦方向は `touch-action: pan-y` でネイティブスクロールを維持し、リンク・ボタン・横スクロール領域では従来どおり無効。
- 切替閾値を 60px から 48px に短縮（後続調整で44pxへ変更）。未達・両端方向では短時間で元位置へ戻す。
- ボトムナビの選択ピルを白背景＋ブランド濃色文字から、ブランド背景＋白文字へ変更。
- シェル変更のため Service Worker の `CACHE_VERSION` を v39→v40 に更新。

### 2026-07-16 ブランド色補正の撤回・スワイプ再調整

- `--brand-solid` の自動計算を撤去。ヘッダー、CTA、進行中表示、ボトムナビは `eventInfo.brandColor` の指定色と `--brand-2` を直接使用する構成へ戻した。
- スワイプ中は現在タブだけでなく、移動先タブも画面外から同時に指へ追従させる方式へ変更。指を離した位置から180msで確定／復帰し、素早いフリックは移動量が短くても速度で判定する。
- 切替距離は44px、速度判定は0.45px/ms。端方向は28%の抵抗を付け、隣タブを表示しない。
- シェル変更のため Service Worker の `CACHE_VERSION` を v40→v41 に更新。

### 2026-07-16 BOOKSスワイプ修正・明るいガラス調ヘッダー

- BOOKSタブの書籍カードがリンクで画面を覆い、`a` 上のスワイプを無効化していたため戻る操作ができなかった。リンク・ボタン上でも横軸ロック後はスワイプを優先し、通常タップはそのまま通す方式へ変更。スワイプ直後の誤クリックだけ400ms以内で抑止する。
- スワイプ距離を42pxへ微調整。現在ビューと隣接ビューの同時追従、速度判定、方向反転、端の抵抗、キャンセル復帰を全タブ共通で維持。
- ヘッダーを「白混色78%の明部→純ブランド色→黒混色86%」へ再構成。明部と純色の面積を広げ、暗部は右下に限定した。
- ヘッダーへ放射状ハイライト、薄いガラスレイヤー、内側ハイライトを追加。テーマバナーは半透明＋blur＋細い白枠に変更し、装飾円も透明度と境界を調整。
- シェル変更のため Service Worker の `CACHE_VERSION` を v41→v42 に更新。

### 2026-07-16 全国国語授業研究大会のブランド色調整

- チラシの明るい水色を参考に、全国国語授業研究大会の主ブランド色を `#00a7d8` から、少し明るく緑寄りのシアン `#00b8c8` へ変更。
- `events.json`、個別イベント JSON、実体マニフェスト、イベント別 PWA アイコンを同色に統一した。
- 初回実装の3イベントでは見分けやすさも考慮したが、今後はイベント自身が使いたいブランドカラーを優先する。
- イベント別データ・アセットのみの変更で、いずれも Network First で更新されるため `CACHE_VERSION` は v42 のまま。

### 2026-07-16 ボトムナビの選択色を明るく調整

- 選択中タブのピルを、従来の「ブランド色→暗い補助色」から、ヘッダーの明部と同じ「白を22%混ぜたブランド色→純ブランド色」のグラデーションへ変更。
- 白文字には控えめなテキストシャドウを加え、明るい側でも輪郭を保つようにした。
- シェル変更のため Service Worker の `CACHE_VERSION` を v42→v43 に更新。

### 2026-07-16 すりガラス表現の整理・ヘッダー光レイヤーのスクロール連動

- blurは背後のコンテンツが動く重なり面に限定。固定ボトムナビとスクロールするモーダルの固定上端には残し、ヘッダー内テーマ帯と通常配置の日付タブからは除去した。
- ボトムナビのblurを24pxから16pxへ抑え、すりガラス感を維持しながら固定レイヤーの描画負荷を軽減。
- ヘッダーの放射状ハイライトを独立した疑似要素へ分離し、スクロール量に応じて最大20px横・8px縦へ移動。スクロールイベントはpassive、更新は`requestAnimationFrame`で1フレーム1回に制限し、再描画の軽い`transform`だけを変更する。
- `prefers-reduced-motion` 環境では光の移動を停止し、`backdrop-filter` 非対応環境では重なり面を不透明背景へフォールバック。
- シェル変更のため Service Worker の `CACHE_VERSION` を v43→v44 に更新。

### 2026-07-16 ヘッダー装飾円のパララックス・選択ピルのすりガラス化

- ヘッダー右側の大小2つの装飾円をスクロール連動に追加。大円は最大12px左・7px下へ、小円はその約1.45倍を横、逆方向へ約0.65倍を縦に動かし、光レイヤーと異なる奥行きを出した。
- 装飾円も光レイヤーと同じ`requestAnimationFrame`内でCSS変数を更新し、描画は`transform`だけに限定。`prefers-reduced-motion`では停止する。
- ボトムナビの選択ピルを半透明のブランドグラデーションへ変更し、8pxの局所blur、彩度補正、薄い白枠、内側ハイライトですりガラス感を追加。
- `backdrop-filter` 非対応環境では不透明な明色ブランドグラデーションへフォールバック。
- シェル変更のため Service Worker の `CACHE_VERSION` を v44→v45 に更新。

### 2026-07-16 全国研の昼休み表示を統一

- 運営系カテゴリの判定が「昼食」「昼休憩」には対応していた一方、全国国語授業研究大会の「昼休み」を認識せず通常カードになっていた。
- 判定語を「昼休」にまとめ、「昼休み」「昼休憩」をどちらも他イベントと同じコンパクト行で表示するよう修正。
- シェル変更のため Service Worker の `CACHE_VERSION` を v45→v46 に更新。

### 2026-07-16 ボトムナビ選択背景の高さ補正

- 選択背景が最小48px固定だったため、スマートフォンのフォント描画や行高によってアイコン＋文字の実高が増すと、文字下端と背景下端が近くなっていた。
- インジケータをナビ領域の上4px・下4pxに固定し、選択背景をその実高いっぱいへ伸ばす方式に変更。端末ごとの文字描画差があっても上下余白が揃うようにした。
- シェル変更のため Service Worker の `CACHE_VERSION` を v46→v47 に更新。

### 2026-07-16 軽量アニメーション・スマホ余白・ヘッダー視差の改善

- タブのタップ切替を、強制レイアウトを伴うCSSアニメーション再実行から Web Animations API に変更。移動方向に合わせた短いスライド＋フェードを `transform` / `opacity` のみで描画する。
- 左右スワイプ中は現在ビューと隣接ビューの透明度も移動量に連動させ、指を離した後の確定／復帰にもクロスフェードを追加した。
- タイムテーブル行へ `content-visibility: auto` を適用し、画面外の長いリストの描画を遅延。未対応ブラウザでは従来表示へフォールバックする。
- ヘッダー装飾の `will-change` は常時指定せずスクロール中だけ有効化し、停止140ms後に解除。スクロール量180px以降は同じCSS変数を再設定しないようにした。
- スマートフォンでクレジット表記がボトムナビへ近すぎたため、イベントモードの本文下余白を16px拡大した。
- ヘッダーの光・右側の大小2つの丸の最大移動距離を約1.5倍へ拡大。奥の光と手前の丸で移動量を変え、スクロール180pxで動き切る従来の上限は維持した。
- `prefers-reduced-motion` によるアニメーション停止は全変更で維持。
- 一連のシェル変更に伴い Service Worker の `CACHE_VERSION` を v47→v50 に更新。

### 2026-07-16 タブ内の主要タイトルを拡大

- TIMETABLE タブの各プログラム内容タイトル（例: 講演1の「見つける・つなぐ・広げる技術の高め方」）を12.5pxから13pxへ拡大後、13.5pxへ再調整。
- FILES タブのリンク項目名（例: 「参加申込み」）を14pxから14.5pxへ拡大。補足説明の文字サイズは変更していない。
- BOOKS タブの書名を14pxから14.5pxへ拡大。著者名・出版社・紹介文の文字サイズは変更していない。
- **今回見送った指摘・提案**: 直前までに挙がっていたその他の提案・追加調整は、この変更には含めず一旦保留とした。今回の対象は上記3種類の文字サイズだけで、レイアウト、文字の太さ、色、余白、データ内容には手を加えていない。

### PR #28 — タイムテーブルの時刻フォント拡大・アイテムの細字小サイズ表示（merged）

- スマートフォンでの視認性向上のため、タイムテーブルの時刻表示を拡大。内容カードの時刻レンジを11px→12.5px、運営系コンパクト行の開始時刻を12.5px→13.5px に。
- `items[]` に汎用フラグ `subtle`（真偽値）を追加。`true` の項目タイトルは細字・小サイズ（11.5px / `font-normal` / `slate-500`）で表示する。特定イベント固有ではなくデータ側で切り替える汎用オプション。
- 算数サマーフェス2日目「算数部OBが集います！」に `subtle: true` を適用。
- シェル変更のため `CACHE_VERSION` を v53→v54 に更新。

### PR #29 — 概要・お知らせモーダルを下スワイプで閉じる（merged）

- 右上の×ボタンに加え、モーダルパネルを強めに下方向へスワイプしても閉じられるようにした（閾値70px、`closeModal()` を共用）。
- 本文が上端まで（`scrollTop === 0`）スクロールされている時のみ有効にし、本文スクロール操作とは競合させない。上方向の動きは対象外。
- リスナは `{ passive: true }` を維持。シェル変更のため `CACHE_VERSION` を v54→v55 に更新。

### PR #30 → #31 — 外部リンクの window.open 化と差し戻し（both merged）

- #30: PWA standalone 起動時に外部リンクを `window.open` で開く処理を追加したが、元々ブラウザ chrome（戻る/閉じる）が出て閉じられていた**書籍リンクまで閉じられなくなる**副作用が判明。
- #31: #30 を差し戻し。書籍・特設販売ページ・資料/フォームの各リンクは、いずれも同一の `target="_blank"` + `rel="noopener"` に統一（特設ページ専用処理は持たない）。
- `CACHE_VERSION` は v55→v56（#30）→v57（#31）。

### PR #32 — サマーフェスに実体 webmanifest を追加し PWA を安定化（merged）

- サマーフェスだけ `eventInfo.manifestPath` が無く、実行時に data URI で動的生成したマニフェストを使っていた。data URI マニフェストは iOS で `scope` の扱いが不安定になり、インストール後のPWAで外部リンク（書籍・関連イベント）を開いた際に元ページへ戻りにくい状態になっていた。
- 全算研・全国研と同じく実体ファイル `events/2026-math-summer-fes.webmanifest`（`display: standalone` / `scope: "../"` / brandColor `#0ea5e9`）と、アイコン `assets/icon-2026-math-summer-fes.svg`（ラベル「算サマ」）を用意し、`eventInfo.manifestPath` で参照するよう統一。
- シェル（`index.html` / `sw.js`）は不変。追加分はイベント個別アセットで Network First キャッシュのため `CACHE_VERSION` は据え置き。
- **知見**: PWA のマニフェストは data URI 生成より**実体 `.webmanifest` ファイルが iOS で安定**（外部リンクの戻り導線にも影響）。新規イベントでも実体マニフェストの用意を推奨。
- シェル変更のため Service Worker の `CACHE_VERSION` を v51→v52、再調整時に v52→v53へ更新。

### 2026-07-17 軽量モダンUIの追加

- **フロストUI**: スクロール中・縮小時のヘッダーを半透明＋10px blurにし、固定ボトムナビと合わせて奥行きを付与。`backdrop-filter` 非対応環境では従来どおり不透明グラデーションへフォールバックする。
- **軽量な動き**: タブ切替の短いスライド＋フェード、タブ内要素の短いスタガー表示、リンク／カードの押下フィードバック、スケルトンのshimmer、書影のフェードイン、通知・ジャンプボタンの一回表示アニメーションを追加。描画は主に `transform`／`opacity` に限定する。
- **当日表示の視認性**: 進行中カードを薄いブランド色背景・枠線・進捗バー・残り時間で強調し、進行中へ切り替わる瞬間だけ擬似要素が一度広がる。常時パルスは廃止した。
- **アクセシビリティ**: `prefers-reduced-motion` 時は新規の装飾アニメーションを停止し、静的な強調表示だけを残す。
- シェル変更に伴い Service Worker の `CACHE_VERSION` を **v57→v59→v60** に更新。

### 2026-07-18 Web プッシュ通知の実装案（検討のみ・没）

- 「静的PWA + サーバーレスバックエンド（Cloudflare Workers等）+ Web Push API」の一般的な実装プランを提示され、t-pod への適用是非を検討。
- **見送りと判断**: バックエンド（購読情報の永続化・送信API・VAPID秘密鍵管理・管理者認証）が必須になり、「静的サイトのみ・ビルド工程なし・フォームすら外部誘導」という設計原則と衝突。単発イベント運用に対して鍵ローテーションや無効購読の掃除などの継続運用コストが見合わない。
- iOS では 16.4+ かつホーム画面追加済みPWAからの許可が必須で、現状ホーム画面追加は任意機能のため到達率がごく小さい点も判断材料。加えてイベント毎に動的生成されるマニフェスト（`manifestPath` 無しの場合）は iOS での安定性に懸念があり、購読をイベント別に分離する必要も生じ複雑化する。
- 代替案: `events/<id>.json` に `notices`（お知らせ）配列を追加しアプリ内表示する案が、Network First キャッシュの特性上「開くたびに最新のお知らせが出る」形で当日周知に実用上足りると判断。即時性が必要な場合は既存方針どおり外部サービス（LINEオープンチャット等）へ誘導。
- コード変更なし（検討のみ、実装は行っていない）。

### 2026-07-21 全体レビューによる堅牢性・アクセシビリティ・スクリプト改善

複数観点で全体を点検し、脆弱性・エッジケースのバグ・アクセシビリティ・スクリプトの堅牢性をまとめて改善した。UI の見た目や運用手順は変えていない。

- **セキュリティ（`index.html`）**: JSON 由来の URL を `href`/`src` に入れる箇所（リンクカード・書籍・特設販売・次回案内・会場マップ・書影）へ `safeUrl()` を導入し、`http`/`https` と相対パスのみ許可（`javascript:` 等のスキームを弾く）。相対パスはそのまま保持するため既存データに影響なし。
- **Service Worker（`sw.js`）**: Play CDN の URL 不一致（`index.html` は `https://cdn.tailwindcss.com`、プリキャッシュは `.../3.4.17`）でオフライン時に全スタイルが崩れる不具合を、`index.html` 側を `3.4.17` 固定にして解消。`networkFirst` を opaque レスポンス（no-cors のクロスオリジン＝Webフォント実体）もキャッシュするよう緩和。`handleNavigate` の `theme-color` 差し替え時に本文長が変わるため `content-length` を除去して不整合を防止。
- **当日ハイライトのバグ（`index.html`）**: `updateNowMarker()` が、当日最終セッションの終了後もブランド枠強調と「▼ いまの予定へ」を残し続けていた。最終セッションの `end`、無い場合は `eventInfo.dates[].time` の終了時刻（`dayEndMin()` で末尾の `H:MM` を抽出）を過ぎたら「現在」扱いを解除するよう修正。
- **エラー UX（`index.html`）**: `events/<id>.json` の取得に `AbortController`＋10秒タイムアウトを追加し、ハング時もスケルトンを残さず `errorBox` へ倒す。`errorBox` に `location.reload()` する「再試行」ボタンを追加。
- **iOS 背景スクロール固定（`index.html`）**: モーダル／LINE ポップアップ表示中の背面スクロールが iOS Safari で止まらなかったのを、`position:fixed`＋scrollY 退避（`lockBodyScroll()`/`unlockBodyScroll()`）で固定し、閉じたら元位置へ復元する標準手法に変更。
- **アクセシビリティ（`index.html`）**: モーダルに Tab フォーカストラップを追加（背面への抜けを防止）。オフラインバナーに `role="status"`。`applyAppIdentity()` を try/catch で保護し、失敗しても表示継続。reduced-motion 判定を `reduceMotionQuery` に一本化。
- **スクリプト堅牢化**: `validate_events.py`／`generate_event_url_index.py`／`import_event_workbook.py` に UTF-8 出力の `reconfigure` を追加（Windows cp932 端末での文字化け・例外落ち防止）。`import_event_workbook.py` の生成アイコン文字色を白固定から輝度判定（`index.html` と同式・閾値 0.55）に変更し、検証失敗時に生成物をロールバックする処理を追加。`find_event.py` のデッド三項と `validate_events.py` の `brandColor` None 安全化。
- 一連のシェル変更に伴い Service Worker の `CACHE_VERSION` を **v74→v77** に更新。

### 2026-07-25 右下のドット絵マスコット「ふせんネコ」

タイムテーブルを読み解かなくても「あと何日か」「いま何の時間か」「明日は何時からか」が伝わるよう、
イベントページの右下に 24×24 のローピクセル・マスコットと吹き出しを常駐させた。

- **ドット絵は実行時生成**: `CAT_BASE`（24行×24文字）＋差分パッチ（`CAT_TAIL_UP` / `CAT_BLINK`）を JS に持ち、`buildCatSvg()` が横方向のランレングス結合で `<rect>` にまとめて SVG 文字列を返す。6コマを横一列に並べ、`translate3d` + `steps(6)` でコマ送りする（`transform` のみ・GPU 合成）。バイナリ資産は追加していない。
- **ブランド色に追従**: 塗りは `<style>` の `.cat-*` クラスが持ち、輪郭は `color-mix(in srgb, var(--brand) 34%, #2b2118)`、体色は `var(--brand)`。`eventInfo.brandColor` を変えるだけで体色が変わる（ほっぺと目は固定色）。`applyBrandColor()` 後の再生成は不要。
- **文言は純関数**: `mascotLines(now = new Date())` が `eventInfo.dates[]` と `sessions[]` から算出する。開催前は「あと◯日だよ！」、当日は「いまは『◯◯』の時間だよ／あと◯分だよ／つぎは ◯:◯◯ から…」、当日終了後は「あしたは ◯:◯◯ からだよ」、全日程終了後は `nextEvent.date` を案内し、終了済み（最終日+7日超・`_status` 優先）は「また来年も参加してね！」。`now` を引数で差し替えられるため、デバッグ用の UI やクエリを足さずにコンソールから全分岐を確認できる。
- **「進行中」判定の改善**: 開始時刻を持たない末尾の行（理事・幹事会など）があると `updateNowMarker()` 由来の「最後の要素か」判定が効かず、`end` 無しの最終セッションが延々と「進行中」のままになる。マスコット側は「以降に開始時刻を持つ行が無いか」で判定し、`end` が無い場合は `dates[].time` の終了時刻（無ければ開始+30分）で締める。
- **表示条件と操作**: イベントページ（`?id=` あり）のみ。`eventInfo.mascot: false` でイベント単位のオプトアウト。タップで吹き出しを畳み、状態は `localStorage["t-pod:mascot-folded"]` に保持。文言は7秒ごとに切り替え、同じタイマーで文言自体も作り直す（`setInterval` は1本）。
- **配置とアクセシビリティ**: `z-20`（ボトムナビ `z-30`・モーダル `z-40` より下）でボトムナビの上に固定。吹き出しは白地＋`text-slate-700` でブランド色ベタ塗りは使わない。7秒ごとに変わる装飾的な一言のため `aria-live` は付けない。`prefers-reduced-motion` ではコマ送りを停止し1コマ目で静止する。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v81→v82** に更新。

### 2026-07-25 ふせんネコの体型・配色・しっぽ・吹き出しの調整

実機で見た指摘4点をまとめて直した。文言ロジック（`mascotLines()`）とイベント JSON は変更していない。

- **胴体を正方形に**: ふせん本体の外枠が 16列×18行で縦長だったのを、**col 2–17 / row 5–20 の 16×16** に作り直した。耳・折れ角・目・口の各パーツを 1 行下げ、下端を 2 行詰めている。
- **配色を淡いパステルに統一**: 黒に近い固定色（輪郭の `color-mix(--brand 34%, #2b2118)`、目の `#2b2118`）を全廃。体を `color-mix(var(--brand) 52%, white)`、折れ角の陰を素の `var(--brand)`、**輪郭・目・口を同系の濃色 1 色**（`color-mix(var(--brand) 70%, black)`）に揃えた。既定の黄 `#ffd900` / amber `#f59e0b` / シアン `#00b8c8` の3色で、体と輪郭が分離することを実測で確認。
- **しっぽを作り直した**: 太さ一定の直角フック（差込プラグのように見えていた）をやめ、胴体右辺（row 14–15 で col 17 の外枠を開けている）から生えて **col 18 を空けてから 3px 幅で立ち上がる弧**にした。胴体の輪郭と1列離すことで「生えているしっぽ」として読める。
- **しっぽの動きを大きくした**: 全体を 1px 上下させるだけだった待機アニメを、**中→上→中→下の 3 ポーズ**（先端が 4 行ぶん動く）に変更。コマ数を 6→8（2.8 秒で一巡）にし、途中で 1 回まばたきする。
- **差分パッチをセル単位の重ね合わせに変更**: 行まるごとの置き換えでは「しっぽを振りながらまばたき」が表現できないため、パッチ文字列に `_`（下の絵を残す）と `.`（透過にする）を導入し、`applyCatPatches()` が複数パッチを合成するようにした。`buildCatSvg()` はパッチ配列を受け取る。
- **吹き出しを背面から分離**: 白いセッションカードの上に重なると輪郭が消えていたので、枠線を `border-slate-300` に、影を `0 6px 18px -4px rgba(15,23,42,.28)` に強め、`#mascotBubble::after`（45° 回転した正方形）で**ネコを指す三角のしっぽ**を追加した。コンテナの `gap` を 1.5→2.5 に広げて三角がネコに重ならないようにしている。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v82→v83** に更新。

### 2026-07-25 ふせんネコのしっぽ位置・輪郭・アニメーションのバリエーション

- **しっぽを 2 行下げた**: 付け根が row 14–15（本体 row 5–20 の約 60%）で頬の高さにあったのを、**row 16–17（約 73%）**へ移した。形は変えず、先端のふた・軸・肘・付け根をまとめて 2 行下げている。
- **輪郭を濃くし、目・口をそれに合わせた**: 「輪郭・目・口を同系の濃色 1 色」だったのを分離し、**体（`--brand` 52%＋白）→ 輪郭（60%＋黒）→ 目・口（50%＋黒）の 3 段**にした。輪郭だけ濃くすると目が沈むため、目・口はさらに一段濃くしている。黄・amber・シアンの 3 色で各段が見分けられ、かつ黒浮きしないことを実測で確認。
- **表情と耳のパッチを追加**: `CAT_SMILE`（にっこり。目を `^` の弧に）、`CAT_SURPRISE`（びっくり。目を 1 行上まで見開き、口を 2×2 の「o」に）、`CAT_EARS_DOWN`（耳ぴく。耳を 1 ドット縮めて耳先を輪郭で塞ぐ）。既存のセル単位パッチなので、しっぽのどのポーズとも自由に重ねられる。
- **コマを 8→16 に**: しっぽの往復に、まばたき・にっこり・びっくり・耳ぴくを別々の拍で差し込んだ（既定 4.8 秒で一巡）。`<rect>` はランレングス結合済みで 16 コマ計 1,702 個。
- **呼吸（体の 1 ドット上下）を追加**: コマ送りが `#mascotStrip` の `translateX` を使っているため、入れ子の `#mascotBob` に `translateY` を持たせた（同一要素で `transform` を二重に持てない）。表示 48px＝24 ドットの 2 倍なので `steps(2)` で 0/2px のちょうど 1 ドットだけ動く。周期 2.2 秒はコマ送り 4.8 秒と割り切れないため、組み合わせが約 26 秒周期になりループ感が薄れる。
- **場面で速さが変わるようにした**: `mascotLines()` の分岐を `mascotState(now)` に切り出して `{ mood, lines }` を返すようにし（`mascotLines()` は `lines` を返す薄いラッパーとして残置。コンソールからの確認手順は不変）、`mood` を `#mascot` の `data-mood` に載せて **CSS 側で `animation-duration` だけ切り替える**。`live`（進行中）3.0 秒 → `today` 4.2 秒 → `before` 4.8 秒 → `after` 6.4 秒 → `ended` 8.0 秒。JS のタイマーは増やしていない。
- `prefers-reduced-motion` では `#mascotStrip` と `#mascotBob` の両方を停止し、1 コマ目で静止させる。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v83→v84** に更新。

### 2026-07-25 吹き出しをドット風に（フォント・枠・しっぽ）

ネコのドット絵と吹き出しの質感が揃っていなかったので、吹き出し側もドット風にした。

- **フォント**: Google Fonts の **DotGothic16**（日本語のドットフォント）を追加し、**吹き出しだけ**に当てた（本文は Zen Kaku Gothic New のまま）。ウェイトは 400 のみなので `font-bold` を外し、合成ボールドでドットが潰れないようにしている。サイズは 13px。Google Fonts は unicode-range で分割配信されるため、必要なサブセットしか落ちてこない。取得に失敗した場合は Zen Kaku Gothic New → monospace にフォールバックする。
- **枠をドット風に**: 角丸と 1px 枠をやめ、9×9 のドット列（`BUBBLE_FRAME`）から作った SVG を `border-image`（`border-image-slice: 3 fill` / `border-image-width: 6px`）で使う。3 マス＝6px なので**ネコと同じ「1 ドット＝2px」の粒**で角が階段になる。辺のスライスは単色にしてあるので `stretch` しても崩れない。
- **三角のしっぽもドット風に**: `BUBBLE_ARROW`（4×6）を `::after` の背景に。左端 1 列が白なので、重なった位置だけ枠が開いて吹き出しから生えているように見える。絶対配置の基準は padding ボックスなので、枠 6px を足した `right: -12px` で「枠に 2px 重なり外へ 6px 出る」位置になる。
- **色はネコの輪郭と共通**: data URI の SVG は `color-mix()` や `var()` を解決できないため、`mixWithBlack(resolvedBrand(), 0.6)`（`.cat-k` と同じ計算）で色を文字列として埋め込み、`setupMascot()` が `--cat-frame`/`--cat-arrow` にセットする。3 イベントすべてで枠の色がネコの輪郭と一致することを実測で確認。
- **影**: ぼかしのある影をやめ、`filter: drop-shadow(2px 3px 0 …)` の 1 段ずらしに。角の階段としっぽの形にも影が沿う。
- **ハマりどころ**: data URI の SVG に `width`/`height` を入れないと固有サイズが無いと見なされ、`border-image-slice: 3` が「画像の 3 ドット」ではなく「3 CSS ピクセル」として解釈されて 9 分割が崩れる（実際に一度崩れた）。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v84→v85** に更新。

### 2026-07-25 待機アニメに毎周のランダム性を追加

16 コマの並びが固定で、見ていると繰り返しに気づいてしまうため、**1 周ごとにコマの中身を選び直す**ようにした。

- **コマは実行時に組み立てる**: 静的な `CAT_FRAMES` を廃止し、`shuffleCatFrames()` が「しっぽのリズム 1 本」＋「表情・耳のアクセント 2〜3 個」から 16 コマを作る。しっぽは `CAT_TAIL_TRACKS`（ふつう／ゆったり／せわしない／ときどき止まる の 4 本）から毎周 1 本を引くので、振り方のリズム自体が変わる。アクセントは空いているコマへランダムに置き、**先頭コマには置かない**（周の切れ目で表情が飛び込んで見えないように）。
- **差し替えのタイミング**: `#mascotStrip` の `animationiteration`。コマ送りが先頭へ戻った直後なので、中身を入れ替えても見た目が飛ばない。タブが非表示の間はイベント自体が来ないので、無駄な処理も起きない。
- **コストを抑える**: 組み合わせは「しっぽ 3 種 × アクセント 5 種」しか無いので、生成した SVG 文字列を `catSvgCache` に貯めて使い回す。さらに前の周と同じ絵のコマは触らないため、毎周の DOM 書き換えはたいてい数コマで済む。
- **mood で傾向を変える**: `CAT_ACCENT_POOL` で重み付けし、開催中（`live`）は「びっくり」「にっこり」が出やすく、終了後（`after`/`ended`）はまばたき中心で**「びっくり」を出さない**。300 周ぶんの集計で意図どおりになることを確認した。
- **呼吸の周期も揺らす**: `jitterCatBob()` が 1 往復ごとに基準値の ±20% ほどで `animation-duration` を書き換える。基準値は CSS（`#mascot[data-mood]`）が持っているので、mood が変わった時だけ読み直す。位相が読めなくなり、コマ送りとの組み合わせがさらに繰り返しに見えにくくなる。
- **ハマりどころ**: アニメーションイベントは親へ伝播するため、`#mascotStrip` の `catIdle` が親の `#mascotBob` のリスナにも届く。両方のリスナで `e.animationName` を見て振り分けている。
- `prefers-reduced-motion` ではコマ送り自体が止まるので、初回に組んだ 1 コマ目のまま作り替えない。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v85→v86** に更新。

### 2026-07-25 待機アニメのアクションを追加（ふだんの動き3種＋まれな来客4種）

「アクセントをもう1,2種増やすのは過剰か」という問いに対し、**密度は種類数と無関係**（`shuffleCatFrames()` は毎周 2〜3 個しか置かない）なので増やしても うるさくならない、と判断して拡張した。増やしたのは「1コマ0.3秒・顔まわり2〜3ドットでも判別できるもの」に限っている。

- **ふだんの動きを 3 種追加**: `look`（よそ見。両目を 1 ドット右へ。左右そろって動くので小さくても分かる）／`sparkle`（わくわく。頭の**外**に光を出すので気づかれやすい。2 コマでちらつく）／`yawn`（あくび。口を大きく開ける。終了後の mood 向け）。**まじめ（目を細める）は入れていない**——0.3 秒ではまばたきと区別がつかないため。
- **まれな来客を 4 種追加**: `butterfly`（ピンク 3×3・弧を描いて頭上を横切る）／`bird`（濃色 5×2・速く直線）／`heart`（ピンク 3×3・上る）／`note`（♪ 濃色 3×4・上る）。24×24 は余白が狭いので「ネコの手前を横切る」前提で描き、**色・大きさ・軌道**で描き分けている。ネコも `CAT_LOOK_UP` で目で追い、最後ににっこりしたり驚いたりする。
- **動きのデータ構造を一般化**: 1 要素＝1 コマぶんの「重ねるパッチ配列」にした（`CAT_ACCENTS.smile = [[CAT_SMILE], [CAT_SMILE]]`）。これでコマごとに位置が変わる来客も同じ仕組みで書ける。位置が動くパッチは `catRow({列: "文字列"})` で組み立てる。
- **出現頻度**: 来客は 1 周につき最大 1 回・`CAT_RARE_CHANCE`（7%）。既定 4.8 秒／周なので **1 分強に 1 回**くらい。2000 周ぶんの集計で実測 7.0〜7.8% を確認。コマ数が多いので、普段の動きより先に場所を取る。
- **mood 別の傾向**（実測）: `live` は わくわく・びっくり・ハート・とり が出る／`before` は よそ見・まばたき中心＋ちょう・とり／`ended` は あくび・まばたき中心で、**びっくり・わくわく・ハート・とりは出ない**（来客は おんぷ と ちょうだけ）。
- `catSvgCache` は 51 エントリで頭打ち（しっぽ 3 種 × 動きのコマ 17 種）。先頭コマが常に素の顔であること・スロットが 16 のままであることを 3000 周ぶん検証した。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v86→v87** に更新。

### 2026-07-25 体の上下を「ときどき沈む」方式に変更

常時 1.1 秒ごとに上下していたのが「ぶれ」に見えて目が疲れる、という指摘への対応。**頻度を下げ、バリエーションは「沈む間隔」で作る**方針に変えた。

- **動きの形を変更**: `steps(2)` の 2 状態トグル（0/2px を半々）をやめ、**ふだんは 0 のまま静止し、1 周の終わりごろに 1 ドットだけ短く沈む**キーフレームにした。`steps(1, end)` は各区間で開始値を保持して終わりで飛ぶので、補間が挟まらず整数ドットに乗る。
- **間隔でバリエーションを作る**: `jitterCatBob()` が 1 回沈むごとに周期を基準の 0.7〜1.7 倍へ振り直す。基準 7 秒（mood 別に 5.5〜12 秒）なので、次に沈むまで **5〜12 秒**とかなり幅が出る。たまに 2 回続けて沈む型（`catBob2`・15%）へ差し替えて、単調さを避ける。
- **実測**（90 秒・rAF サンプリング）: 変更前は 1.1 秒ごとに切り替わり、**沈んでいる時間が全体の 50%**。変更後は **平均 8.2 秒に 1 回**・沈んでいる時間は **7.6%**、間隔は 5.2〜11.2 秒に散った。
- **見つけたバグ**: 走行中に `animation-duration` を書き換えると、CSS アニメーションは「累計経過 ÷ 新しい周期」で回数を計算し直すため、**周期を短くした瞬間に iteration がまとめて消化される**。実測で間隔 0 秒の発火が連続し、90 秒で 25 周・19 回沈んでいた。`animation-name` を一度 `none` にして強制リフローで確定させ、累計経過ごとリセットしてから入れ直すことで解消（同じ 90 秒で 11 周・11 回沈む、間隔 0 秒はゼロ）。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v87→v88** に更新。

### 2026-07-25 マスコットまわりの棚卸し（不具合修正・演出・アクセシビリティ）

実装を一通り点検し、実測で確認できた問題をまとめて直した。

- **mood 変更でコマ送りが飛ぶ不具合を修正**: 呼吸で見つけたのと同じ「走行中に周期が変わると累計経過が再解釈される」問題が、mood 変更時の `#mascotStrip` でも起きていた（実測: 120ms のあいだに `animationiteration` が 2 回発火し、コマ位置が -288px → -336px へジャンプ）。再スタート処理を `restartCatAnim()` に共通化し、mood が変わったらコマ送りと呼吸の両方を先頭から仕切り直すようにした（位置ジャンプは解消。周期変更に伴う 1 回だけの再シャッフルは残るが、これは毎周やっている処理と同じで実害がない）。
- **アクセントの取りこぼしを修正**: 置き場所が衝突すると 1 回で諦めていたため、**置こうとした動きの 2 割強が消えていた**（実測: 1 周あたり 1.93 個しか置けていない）。空きが見つかるまで数回やり直すようにして **2.53 個**（目標 2〜3・平均 2.5）に回復した。
- **しっぽのリズムの連続重複を回避**: 4 本からランダムに引くので **26.6%** の確率で 2 周続けて同じリズムになっていた。前周と別の 1 本を引くようにして **0%** に。
- **初回描画を待たせないようにした**: 16 コマぶんの SVG 解析は実測 6.8ms。`requestAnimationFrame` で初回描画の後ろへ回し、組み上がってから `pop-in` で登場させる（既存の `.pop-in` を再利用。`prefers-reduced-motion` では既に停止対象）。
- **つつくと反応するようにした**: タップで `reactCatFrames()` が先頭コマへ「びっくり／にっこり／わくわく」のどれかを入れ、コマ送りを先頭から回し直す。「いま何コマ目か」を調べる必要がなく、1 周後の `shuffleCatFrames()` が自然に元へ戻す。
- **よそ見（左）を追加**: 既存の「よそ見」は右（しっぽ側）だけだったので、吹き出しの方を見る `CAT_LOOK_L` を足した。ふだんの動きは 8 種に。
- **アクセシビリティ**: 開閉ボタンに `aria-expanded`（畳み状態と同期）と `aria-controls` を付け、`focus-visible` のリングを追加した。
- **堅牢化**: コマの差し替えループにスロットの範囲ガードを入れた。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v88→v89** に更新。

### 2026-07-25 体の上下を「定期的な呼吸」に戻す

「細かくブレるバグがなくなったので、定期的に沈むモーションはあってよい」との判断を受けて、間隔のばらつきを詰めた。

- 疲れる原因だったのは**周期の短さではなく、沈んでいる時間の割合**（旧実装は 50%）と、周期を書き換えるたびに iteration がまとめて消化されるバグだった。どちらも解消済みなので、規則正しい間隔に戻してよい。
- 揺らぎを **0.7〜1.7 倍 → ±12%** に縮め、基準周期も **7 秒 → 5 秒**（mood 別に 4〜8 秒）に短縮した。2 回続けて沈む型（`catBob2`）は 15% → 12%。
- **実測**（90 秒）: 間隔は **4.4〜5.6 秒**に収まり（平均 4.5 秒に 1 回）、規則正しい呼吸に見える。沈んでいる時間は **8.5%** のままなので「細かくぶれている」感じは戻っていない。間隔 0 秒の発火もゼロ。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v89→v90** に更新。

### 2026-07-25 総合点検（「進行中」判定の取りこぼしと日付整形の NaN を修正）

マスコット以外も含めて実装を点検し、実測で確認できた不具合を直した。

- **「▼ いまの予定へ」が終業後もずっと出たままになる不具合を修正**（`updateNowMarker()`）。最終セッションかどうかを**配列の末尾で判定**していたため、**開始時刻を持たない行**（全算研1日目の「理事・幹事会」など）が後ろに並ぶと末尾に一致せず、`end` の無い最終セッション（「事務連絡」16:15〜）が**日付が変わるまで「現在」のまま**残っていた。実測で 18:00 も 22:30 も「▼ いまの予定へ」が出ており、レールの点も光ったままだった。マスコット側（`mascotState()`）には既に入れてあった「これ以降に開始時刻を持つ行が無いか」で判定する方式に揃え、`end` が無い場合の締めも「`dates[].time` の終了時刻、無ければ開始 +30 分」で一致させた。境界を実測し、16:20〜16:40 は「事務連絡」で点灯、16:50 以降は消灯と、**タイムテーブルとマスコットの表示が完全に一致**することを確認した。
- **日付整形が「NaN月NaN日」を出す不具合を修正**（`fmtMD()`）。`split("-")` して数値化するだけだったため、壊れた値（`"not-a-date"` → 「NaN/NaN」）や ISO 日時（`"2026-08-04T10:00"` → 「8月NaN日」）が日付タブや吹き出しにそのまま出ていた。形を検査してから使うようにし、ISO 日時は「8月4日」として受け付け、解釈できない値は空文字を返すようにした。

点検して**問題が無かった**ことを確認した項目:

- **エスケープ漏れ**: 全フィールドに `"><img src=x onerror=…>` を仕込んだイベントを作り、全タブ・お知らせモーダルまで開いて確認。スクリプト実行・要素注入・不正な `href` のいずれも無し。
- **壊れたデータでの例外**: 日付なし／セッションなし／時刻が壊れている／日付が壊れている／7日間／開始時刻を持つ行が1つも無い、の6パターンで例外ゼロ・表示崩れなし・UI に NaN なし。
- **一巡操作**: 3イベント × 時刻6パターンで、全タブ切替・全日付切替・お知らせモーダルの開閉を通してコンソールエラーなし。モーダルを閉じた後に `body` のスクロールロックが残らないことも確認。
- 存在しない `?id` はエラー表示に落ちる（コンソールの 404 は想定どおり）。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v90→v91** に更新。

### 2026-07-25 マスコット一連の作業のまとめ

「ページの右下に、小さく粗いドット絵のアニメを表示したい」という依頼から始まった一連の作業（PR #45〜#53）をここで区切る。

**入ったもの**

| PR | 内容 |
| --- | --- |
| #45 | マスコット本体と、イベントデータから文言を作る `mascotLines()` |
| #46 | 胴体を正方形に／淡いパステルに統一／しっぽ作り直し／吹き出しを背面から分離 |
| #47 | しっぽを下げる／輪郭と目・口の3段階調／16コマ・呼吸・場面別の速さ |
| #48 | 吹き出しをドット風に（DotGothic16・9スライスの枠・三角のしっぽ） |
| #49 | 1周ごとにコマをランダムで組み直す |
| #50 | アクション追加（ふだん7種＋まれな来客4種） |
| #51 | 体の上下を「ときどき沈む」方式に（`animation-duration` の書き換えバグも修正） |
| #52 | 棚卸し（mood 変更でのコマ飛び・アクセント取りこぼし・初回描画・タップ反応・a11y） |
| #53 | 呼吸を定期的に戻す／総合点検（「進行中」判定の取りこぼし・日付の NaN） |

**最終的な仕様**: ドット絵・吹き出しの枠・三角のしっぽはすべてコード内のドットデータから実行時生成（バイナリ資産ゼロ）。色はブランド色に追従。動きはふだん 8 種＋まれな来客 4 種で、16 コマを 1 周ごとにランダム編成。場面（`before`/`today`/`live`/`after`/`ended`）で文言・出る動き・アニメの速さが変わる。文言は `index.html` に固有文言を持たず、すべてイベントデータから算出する。

**この作業で得た知見（再発しやすいので明記）**

1. **CSS アニメの周期を走行中に書き換えてはいけない**。「累計経過 ÷ 新しい周期」で回数が計算し直され、`animationiteration` がまとめて消化されてコマが飛ぶ。`restartCatAnim()` のように `animation-name` を一度 `none` にして強制リフローで確定させ、累計経過ごとリセットする。呼吸（#51）と mood 変更（#52）の 2 箇所で同じ罠を踏んだ。
2. **アニメーションイベントは親へ伝播する**。`#mascotStrip` の `catIdle` が親 `#mascotBob` のリスナにも届く。リスナは必ず `e.animationName` で振り分ける。
3. **data URI の SVG には `width`/`height` が要る**。固有サイズが無いと `border-image-slice` の数値が画像のドット数ではなく CSS ピクセルとして解釈され、9 分割が崩れる。
4. **「配列の末尾か」で最終セッションを判定しない**。開始時刻を持たない行（理事会など）が後ろに並ぶと効かない。「これ以降に開始時刻を持つ行が無いか」で見る。同じ判定が `updateNowMarker()` と `mascotState()` の 2 箇所にあるので、片方だけ直すと表示が食い違う。
5. **ドット絵の調整は必ず拡大 PNG に焼いて目視する**。24×24 では 1 ドットの違いが印象を大きく変え、コードを読むだけでは判断できない。実寸 48px でも併せて見る。

**残課題（今回は手を付けていない）**

- 開催日をまたいで開いたままにすると、日付タブは開いた時点の日のままになる（マスコットの文言は実日付で動くため、深夜〜翌朝は両者がずれる）。自動で切り替えるとユーザーの操作を奪うため、意図的に据え置いている。
- マスコットの文言は `mascotState()` の分岐が増えてきているので、イベント側から文言を差し替えたくなった場合は JSON にフックを設ける設計を検討する（現状はコード内の汎用文言のみ）。

### 2026-07-25 PWA アイデンティティまわりの実害バグ修正

「実害のあるバグを探索して修正」の依頼を受け、これまで深く見ていなかった領域（PWA マニフェスト／アイコン生成、インストール導線、スワイプ、スクロールロック、Service Worker、Python ツール）を点検した。`applyAppIdentityInner()` に実害のある不具合が 2 件見つかり、いずれも「ホーム画面に追加」に効くため修正した。

- **`logoMain` を持たないイベントでマニフェストが 404 のまま残る不具合を修正**。`<head>` の inline script は `?id` が妥当なら**実体の有無に関わらず** `events/<id>.webmanifest` を貼る（iOS がパース時に読むためこの設計が要る）。実体の用意は `applyAppIdentityInner()` の責務だが、同関数は `if (!info.logoMain) return;` で**マニフェストに触る前に返って**いたため、`manifest.json` へ戻らなかった。CLAUDE.md / README / requirements はいずれも「`logoMain` 未指定は静的 `manifest.json` にフォールバック」と書いており、**実装が仕様と食い違っていた**。実害は、マニフェストが無いことで **Android の `beforeinstallprompt` が発火せず**、`showInstallBanner()` の条件に落ちて「ホーム画面に追加」バナーが一切出なくなること。現行3イベントはいずれも `logoMain` を持つため潜在だったが、`logoMain` は任意項目なので次に未指定のイベントを足した時点で発現する。マニフェスト解決を早期 return より前へ移し、共通処理 `setManifestHref()` を切り出した。
- **favicon の `type` が差し替わらない不具合を修正**。静的タグは `type="image/jpeg"` 宣言なのに `href` だけを SVG data URI に替えていた。ブラウザは `type` をアイコン選択のヒントに使うため、宣言と実体の食い違いでイベント別アイコンが採用されず、タブに東洋館ロゴが残ることがある。`type` も一緒に更新するようにした。
- **存在しない `?id` のエラーページでもマニフェストが 404 のまま**だったのを、同じ `setManifestHref()` で `manifest.json` へ戻すようにした（同根・軽微）。

**修正前後の実測**（一時イベントを作って `link[rel=manifest]` の解決先を fetch して確認）:

| ケース | 修正前 | 修正後 |
| --- | --- | --- |
| `logoMain` 無し | `events/_nologo.webmanifest` → **404** | `manifest.json` → **200** |
| 存在しない `?id` | `events/does-not-exist.webmanifest` → **404** | `manifest.json` → **200** |
| `manifestPath` あり（現行3件） | `events/<id>.webmanifest` → 200 | 変わらず 200（回帰なし） |
| favicon の `type` | **`image/jpeg`**（href は SVG data URI で不一致） | `image/svg+xml` |

点検して**問題が無かった**ことを確認した項目:

- **`events/*.webmanifest` の内容**: `start_url` / `scope` の `../` は `events/` 基準でサイトルートへ正しく解決される。3件とも整合。
- **Service Worker**: `handleNavigate` はキャッシュに素のシェルを入れてから theme-color を差し替えるため、オフライン時も正しく動く。`content-length` の削除も妥当。
- **スワイプ**: 軸判定の前に `preventDefault()` を呼ばず縦スクロールを妨げない。`{ passive: … }` の指定も適切。
- **スクロールロック**: 二重ロック・二重解除を防いでおり、LINE ポップアップとモーダルは同時に開かない。
- **`safeUrl()`**: `javascript:` / `data:` を弾く（`mailto:` / `tel:` も弾くが、現状の用途では意図どおり）。
- **セッション時刻**: `validate_events.py` の `TIME_RE` が `25:70` や `9:5` を CI で弾いている。
- **`2026-math-summer-fes` の終了時刻の食い違い**（`dates[].time` 15:10 と実セッション 15:15、`events.json` と個別 JSON の `dateRange` 不一致で CI が WARN 1 件）: どちらが正しいか判断できず、かつ終了したイベントなので**触らない**方針を確認済み。WARN は残る。
- シェル変更のため Service Worker の `CACHE_VERSION` を **v91→v92** に更新。

### 2026-07-25 ふせんネコを設定資料の造形で作り直し、定義を 1 箇所に集約

「設定資料（ふせんネコ 24px ベース）を正解として、いまの絵を調整するのではなく基本造形を作り直す」という依頼。これまでの絵は**「右上が折れた付箋＋ネコ」から離れ、一般的な四角いネコ**に寄っていた——胴体が縦長、耳が大きく左右ほぼ対称、右上の折れが大きな三角 1 枚、しっぽが太い、口が横に広い。

**造形を作り直した**（PR #56、追補 #57）。資料の画像を 24×24 のグリッドとして 1 ドットずつ読み取り（Playwright で canvas に描いて実測）、そこへ合わせている。

- **胴体** col 1–17 / row 5–19 の 17×15。ほぼ正方形でわずかに横長。**幅を奇数にした**ので中心が col 9 ちょうどになり、顔の左右対称を整数で取れる（16 幅だと中心が 9.5 になり、目・鼻・口のどれかが必ず半ドットずれる）。
- **右上** 耳を立てず、ふせんの角を**下へ折り返す**。1 ドットずつ階段状に切り落とし、内側を折り返し色で塗って横線と縦線で閉じる。大きな三角 1 枚にはしない。
- **左耳** 小さな階段。右とは対称にしない。耳ぴくも左耳だけ動かす。
- **顔** 目＝1 ドット幅 × 3 ドット高の細い縦棒、口＝上段の 3 点＋下段の帯で ω。**鼻はほっぺの色ではなく目・口と同じ濃色**（ピンクにすると猫に見えない）。
- **しっぽ** 中身 1 ドット幅の短い鉤形。
- 輪郭と目・鼻・口を濃くした（黒混合 40%→62% / 50%→74%）。

**構造を組み替えた**。造形の定義は `assets/fuseneko/fuseneko-grid.js` の 1 箇所だけになり、`index.html` は `<script src>` で読んで待機アニメ用に束ねるだけになった。配布用 SVG（`fuseneko-{base,standard,smile,serious,happy,idea}.svg`）は `scripts/build_fuseneko.py` が同じ定義から生成し、ずれれば `tests/test_fuseneko.py` が CI で落とす。色は `--cat-base`/`-fold`/`-outline`/`-face`/`-cheek` の 5 変数に統一。確認ページ `dev/fuseneko.html` を追加。

**依頼の仕様から外したもの**（いずれも実装して実寸で見たうえでの判断）:

- **`.tsx` / `.ts` ではなく Vanilla JS**。このリポジトリは `package.json`・`src/`・TS 設定を持たず、ビルド工程なしで GitHub Pages を配信しているため TSX はそのまま動かない。ファイル分離・単一定義・props 相当の API という意図は満たしている。
- **教科小物と番号バッジは載せない**。24px では小物に胴体左下の数ドット、バッジの数字に 3×3 しか取れず、どちらも判別できなかった。`item` / `badge` の props ごと持たせていない。

**総点検で見つけて直した不具合**:

| 症状 | 原因 | 実害 |
| --- | --- | --- |
| 造形定義が読めないと**タイムテーブルごと真っ白**（「読み込み中…」で停止） | 巨大なインライン script の途中で `FN.EXPRESSIONS` を素で触っており、undefined で以降の描画ロジックごと落ちていた | 会場の不安定な通信で本文まで巻き添え。`FN_OK` で通し、`setupMascot()` がマスコットだけ諦めるようにした |
| 吹き出しの枠がネコの輪郭より薄い | 輪郭を濃くした際、`mixWithBlack(…, 0.6)` の側を更新し忘れて古い比率のままだった | 枠だけ浮いて見える。0.38 に揃え、両方にコメントを置いた |
| 「うれしい」で**鼻が消える** | 口を差し替えるパッチが row 14 をまとめて上書きし、鼻（col 9）ごと塗り潰していた | 猫の顔に見えない。跳ねだけ落とすよう修正し、**全表情で鼻が残ることを検査するテスト**を追加 |
| 色違いが**全部同じ色**になる | インライン SVG の `<style>` は文書全体に効くため、同じページに並べると最後の 1 枚が全部を上書きしていた | 色はインスタンスごとの CSS 変数で渡すよう変更 |

**未実装だった表情を追加**。資料の 6 表情のうち「うれしい」（にっこりの目＋舌の見える開いた口）と「ひらめき」（標準の顔＋頭の外の電球）が入っていなかったので足し、待機アニメのプールにも組み込んだ。

**動きの豊富さは旧実装と同等以上**であることを実測で確認した。旧版（d4e098f）を worktree に立てて同じ計測を流し、アクセント 8 種・来客 4 種・しっぽ 4 本・来客率 7%・16 コマ・反応 3 種・`catBob2` 12% と、mood ごとの出現種類までが**完全に一致**。そのうえで「うれしい」「ひらめき」が加わり、1 周あたりの異なり数は 3.49 → 3.73 に増えた。

**そのほか確認したこと**: 通常ロード・案内モード・不正 `?id`・パストラバーサル・`prefers-reduced-motion` で実行時エラーなし。全表情 × しっぽ 3 姿勢（42 通り）でグリッド外に出るドットなし。描画結果をキャンバスへ読み出し、**出現色がパレット色ちょうどだけ（中間色 0 件）**であることを DPR1・DPR2 の両方で確認（アンチエイリアスなし）。

**残っている既存事象**: 存在しない `?id` のとき、ヘッダーの見出しが「読み込み中…」のまま残る。旧版（d4e098f）でも同じ挙動で、今回の変更由来ではない。別途の課題として残す。（→ 次節で消化。なお当初この件を「エラー UI が出ない」と書いたのは**誤り**で、エラー枠と「再試行」「案内ページへ戻る」は出ていた。本文の innerText を 70 文字で切って読んでいたための誤読。）

### 2026-07-26 申し送り 2 件の消化（読み込み失敗時の見出し／サマフェスの終了時刻）

ふせんネコの一連（#56 / #57）を締めたときに「今回の変更由来ではないが未解決」として残した 2 件を消化した。

**① 読み込みに失敗したとき、見出しが「読み込み中…」のまま残る**

`init()` の catch は `setMode("list")` → マニフェストを汎用へ戻す → `#errorBox` 表示、と進むが `#eventTitle` を書き換えていなかった。エラー枠が「読み込みに失敗しました」と言っている真上で見出しが「読み込み中…」のままになり、矛盾して見える（`document.title` も同様）。`renderAccessGuide()` が持っていた 3 行を `setPageHeading(text, subtitle)` に切り出し、案内モードとエラー時の両方から通すようにした。

エラー時の文言は **「イベントを読み込めませんでした」** と中立にした。この分岐は「存在しない id」だけでなく**通信失敗・10 秒タイムアウトでも通る**ため、「見つかりません」と断定すると電波が悪いだけのときに誤った案内になる。原因の説明と導線（再試行／案内ページへ戻る）は従来どおり `#errorBox` が持つ。

**② `2026-math-summer-fes` の終了時刻の食い違い（CI 警告 1 件）**

前回「どちらが正しいか判断できない」として保留していたが、**個別 JSON 自身のセッションデータで決着した**。2 日目の最後の実セッションが「講演２ 14:25〜15:15」で、その直後に `{"start": "15:15", "category": "終了予定"}` の行がある。個別 JSON の `dateRange` も既に 15:15。つまり **15:15 が正**で、15:10 と書いてある 2 箇所が古い。

`validate_events.py` の `normalize_display_text()` が全角半角を吸収するため、**実際の差は「15:10 か 15:15 か」の 1 箇所だけ**だった（`７` と `7` の違いは警告の原因ではない）。

| 直した箇所 | 修正前 | 修正後 |
| --- | --- | --- |
| `events.json` の `dateRange` | `…7月19日(日) 15:10` | `…7月19日(日) 15:15` |
| `events/2026-math-summer-fes.json` の `dates[]` day2 `time` | `09:00〜15:10` | `09:00〜15:15` |

**これは表記ゆれの解消だけでなく実害の修正でもある**。`dates[].time` は表示専用ではなく、`dayEndMin()` が「その日の終了時刻」として解釈し、`end` を持たない最終行を締めるのに使っている（`updateNowMarker()` と `mascotState()` の両方）。修正前は **`dayEndMin(day2)` = 15:10 に対し最終行の開始が 15:15** で、自分の開始より前の時刻で締めていた。修正後は 15:15 で一致する（ブラウザのコンソールで実測。`endBeforeStart: true → false`）。

`events.json` を触ったので `docs/event-url-index.md` の再生成が必要になった（`validate_events.py` が ERROR で検出）。`scripts/generate_event_url_index.py` で更新。

**実測した結果**

| ケース | 見出し | エラー枠 | 導線 |
| --- | --- | --- | --- |
| `?id=does-not-exist` | イベントを読み込めませんでした | 表示 | 再試行／案内ページへ戻る |
| `?id=2026-math-summer-fes` | 算数サマー2026 | 非表示 | — |
| `?id=2026-zensanken-37` | 全算研2026 | 非表示 | — |
| `?id` 無し | イベントページのご案内 | 非表示 | — |

いずれも実行時エラーなし、スケルトンは全ケースで非表示。`validate_events.py` は**エラー 0・警告 0**（従来は警告 1 件）、テスト 12 件成功。シェル変更のため `CACHE_VERSION` を v95 → v96。

**触っていない残課題**: 日付タブの自動切替（開催日をまたいでも開いた時点の日のまま）とマスコット文言の JSON フックは、いずれも「意図的に据え置き」と記録されているため今回の範囲外。
